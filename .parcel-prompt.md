# Big parcel lane: graphics  (converge to COMPLETION)

FIRST: print your working directory. It MUST be named `BPTK-lane-graphics`. If not, STOP — you are in the wrong tree. Repo: /Users/angelonrevelo/Code/BPTK-lane-graphics (branch lane/graphics, off build/product-first @ 11afff3, which already contains the merged thread/seh/cpu/usergdi/audio/net runtime — 43 implemented, 0 passing).

## THIS IS A CONVERGE-TO-COMPLETION RUN — do not stop at a slice
Build EVERY item in your scope to its acceptance, gate-green, iterating for HOURS, until your area is COMPLETE (all your planned/red items implemented with their active red contract + their conformance complete + `npm run gate` exit 0) or the budget ends. Each cycle: re-derive your remaining planned/red items from bench/roadmap/manifest.json + doc/roadmap-gold-standard.json (only items whose owned files are in YOUR list); pick the next by dependency + corpus/ledger leverage; implement it GENERICALLY (never if(game===)); measure (bptk corpus run / coverage / conformance); /gate; commit one focused change; repeat. Never stop because one cycle didn't move a real .exe's stage.

## VERIFIER (only thing that says an item is done)
`npm run gate` exits 0 after every commit. Run it, read the exit code. Never claim complete on a budget ceiling.

## YOUR SCOPE
PILLAR 2 (graphics, one unified layer). Follow the PINNED 2026 toolchain in doc/roadmap-gold-standard.md: ONE SPIR-V→WGSL emitter (reuse Tint/naga), per-API frontends REUSED offline (DXVK dxbc-spirv for D3D SM1-6, dxil-spirv for DXIL, DXVK dxso for D3D9 SM1-3), hand-written FFP + Glide generators, ANGLE as GL-path reference, emdawnwebgpu runtime backend. Build in lib/shader.mjs + lib/graphics.mjs: the unified IR (GS-018), honest caps red-by-construction (GS-024), FFP-once (GS-022), texgen/shadow correctness (GS-023), frame-diff regression with TEST-TIME references (GS-025, no committed golden), WebGPU/WebGL2/compat-mode backends (GS-026/027/028), present/gamma/MSAA, pipeline cache, hostile-bytecode containment (GS-033). Translation is BUILD-TIME only (browser WebGPU is WGSL-only). DXBC/DXIL D3D10-12 (GS-019/031/032) is P4 — prototype only.

## FILE OWNERSHIP (why the lanes don't collide)
- YOU OWN (create/edit freely; put NEW logic in NEW files under your prefix): lib/shader.mjs, lib/graphics.mjs
- FORBIDDEN (other lanes own these — do NOT touch): lib/runtime.mjs, lib/i386.mjs, lib/thread.mjs, lib/seh.mjs, lib/performance.mjs, lib/hle.mjs, lib/user.mjs, lib/gdi.mjs, lib/audio.mjs, lib/net.mjs, lib/input.mjs, lib/storage.mjs, lib/clock.mjs, lib/ingest.mjs, lib/census.mjs, lib/port.mjs, lib/foundation.mjs, lib/extract.mjs, lib/import.mjs, lib/pe.mjs, lib/conformance.mjs, lib/corpus.mjs, lib/benchmark.mjs, lib/report.mjs, lib/inspect.mjs, lib/research.mjs, lib/package.mjs, lib/legal.mjs, lib/security.mjs, lib/bound.mjs, lib/platform.mjs, lib/doctor.mjs
- SHARED (edit ONLY your own additive rows; union-merged sequentially): tool/validate.py (the 3 registry places), bench/npm/content.json, bench/roadmap/manifest.json, bench/roadmap/spec/, bench/roadmap/candidate.json, bench/roadmap/fixture/catalog.json, ROADMAP.md, README.md, doc/roadmap-rejected.md, doc/source-audit.md, doc/roadmap-gold-standard.md, data/status.json, test/cli.test.mjs, the createWin32Hle() interface between runtime.mjs and hle.mjs (do not change its signature without coordinating)

## GATE FILE SURFACE — the trap that fails the gate (do this for EVERY new file)
A new `lib/<x>.mjs` or `test/<x>.test.mjs` fails `npm run gate` until registered in THREE places, in the SAME commit:
  1. `tool/validate.py` — the ALLOWED_MJS_PATH / APPROVED_SURFACE set: add Path("lib/<x>.mjs") and Path("test/<x>.test.mjs").
  2. `tool/validate.py` — the embedded reviewed-tarball file list inside validate_package (~line 389): add the new LIB file (test files are not packaged).
  3. `bench/npm/content.json` — the file array: add the new LIB file only.
Keep entries alphabetically near neighbors. (These are shared registries — add ONLY your own entries.)

## PROMOTION (when you implement/advance a roadmap item)
Promote GS→BPTK via the 8-file contract in doc/roadmap-gold-standard.md, keeping counts reconciled (accepted/implemented/raw agree; regenerate data/status.json via `node script/status.mjs`; update test/cli.test.mjs's implemented count; passing MUST stay 0). Add ONLY your own rows to the shared roadmap files.

## HONESTY (non-negotiable — the whole point of this repo)
0 passing stays 0 until a REAL benchmark passes. implemented = real code + an ACTIVE red acceptance contract; each spec states "implemented but red because…". "reached stage X" is never "playable". No false green — the gate is the judge, not your narration. Never delete real code to make the gate pass — reconcile it.

## LEGAL RAILS
DRM-free / offline / redistributable freeware ONLY. DETECT-and-REFUSE DRM / anti-cheat / online titles by identity (never circumvent, never touch a live service). Payloads and traces out of git, provenance/PII-free. Scan and bound every download/extract/run.

## CLOSE-OUT
Keep going until your area is complete or the budget ends. Write doc/graphics-log.md each cycle (items done, corpus/coverage delta, remaining). Then close your lane: `node ~/Code/booted/bin/lane.mjs done --here --by claude`. Do NOT merge yourself — merges happen at the tip in order execution → win32-hle → graphics → lanes-formats → correctness-corpus → platform-eco-sec (execution first — the CPU/memory floor).
