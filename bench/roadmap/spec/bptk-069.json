{
  "benchmark_id": "BENCH-069",
  "item_id": "BPTK-069",
  "kind": "runtime",
  "threshold_owner": "BPTK-002",
  "state": "red",
  "gate": "active",
  "exclusion_reason": "The WebGPU compatibility-mode backend is implemented: the declared subset lowers through the one emitter and an out-of-subset construct (storage, atomic, workgroup, or storage texture) is caught at emit rather than failing on a live device. The benchmark remains red because rendering the subset on a live Chrome 146 compatibility-level adapter does not run in the gate.",
  "prerequisite": [],
  "promotion_trigger": "Promote when the declared subset renders on the compatibility-mode feature level and out-of-subset emission fails at emit.",
  "expected_failing_assertion": "The declared subset renders on a live compatibility-mode feature level.",
  "owner_item": "BPTK-069",
  "active_gate_evidence": "The graphics suite proves the compatibility backend lowers the declared subset through the one emitter and catches an out-of-subset construct at emit.",
  "fixture": [
    "FIX-010"
  ],
  "procedure": [
    "Render the declared subset with the compatibility feature level requested.",
    "Assert out-of-subset emission is caught at emit time."
  ],
  "pass_criterion": [
    "The subset renders on the compatibility level.",
    "Out-of-subset emission is caught at emit."
  ]
}
