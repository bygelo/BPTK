{
  "benchmark_id": "BENCH-003",
  "item_id": "BPTK-003",
  "kind": "runtime",
  "threshold_owner": "BPTK-002",
  "state": "red",
  "gate": "active",
  "exclusion_reason": "The live product self-check and promotion validator exist, but legal approval and the approved corpus denominator remain red.",
  "prerequisite": ["BPTK-001", "BPTK-002"],
  "promotion_trigger": "Pass when the live runner and promotion validator succeed in CI after BPTK-001 and BPTK-002 pass, without a retained result.",
  "expected_failing_assertion": "BPTK-001 and BPTK-002 remain red, so the implemented live runner cannot promote roadmap compatibility state.",
  "owner_item": "BPTK-003",
  "active_gate_evidence": "node --test test/benchmark.test.mjs runs an in-memory deterministic self-check; python3 tool/validate.py enforces state tuple, prerequisite, count, and content-addressed status rules.",
  "fixture": [],
  "procedure": ["Generate the benchmark input in memory and run the same digest operation twice.", "Verify content-addressed status and count invariants.", "Run the repository gate without writing a baseline or result artifact."],
  "pass_criterion": ["Both in-memory runs agree and the status invariants pass.", "The repository gate rejects invalid promotion metadata.", "BPTK-001 and BPTK-002 are passing."]
}
