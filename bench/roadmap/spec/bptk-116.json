{
  "benchmark_id": "BENCH-116",
  "item_id": "BPTK-116",
  "kind": "runtime",
  "threshold_owner": "BPTK-002",
  "state": "red",
  "gate": "active",
  "exclusion_reason": "The authoring SDK surface is implemented as the package entry export set with a committed semver manifest, and the planning gate fails when the live entry exports differ from the committed API. The benchmark remains red because the four-host invariant proof needs the web-component and iframe host shapes that are still absent.",
  "prerequisite": [],
  "promotion_trigger": "Promote when the SDK-only game runs in every host shape under the invariant and the semver contract is enforced in CI.",
  "expected_failing_assertion": "No public SDK exists.",
  "owner_item": "BPTK-116",
  "active_gate_evidence": "The corpus suite compares the live entry exports with the committed manifest exactly, and the SDK contract check in the planning gate fails on any removal or rename.",
  "fixture": [
    "FIX-017"
  ],
  "procedure": [
    "Build the SDK-only fixture game.",
    "Run it in every declared host shape and compare the invariant.",
    "Break the API in a drill and assert the contract check fails."
  ],
  "pass_criterion": [
    "The SDK-only game runs everywhere.",
    "The semver contract is enforced."
  ]
}
