{
  "benchmark_id": "BENCH-063",
  "item_id": "BPTK-063",
  "kind": "runtime",
  "threshold_owner": "BPTK-002",
  "state": "red",
  "gate": "excluded",
  "exclusion_reason": "No GPU translator or fixed-function path exists.",
  "prerequisite": [],
  "promotion_trigger": "Promote when the declared state matrix is generated by one implementation and identical cross-API state yields identical output.",
  "expected_failing_assertion": "No fixed-function pipeline exists.",
  "owner_item": "BPTK-063",
  "active_gate_evidence": "The graphics probes and roadmap keep all fixed-function benchmarks red.",
  "fixture": [
    "FIX-011",
    "FIX-012"
  ],
  "procedure": [
    "Generate the declared light, stage-operation, fog, and alpha matrix from the one generator.",
    "Render identical state through each declared API frontend.",
    "Compare outputs for identity."
  ],
  "pass_criterion": [
    "Identical state yields identical output across API.",
    "The matrix covers the declared combination set."
  ]
}
