{
  "benchmark_id": "BENCH-114",
  "item_id": "BPTK-114",
  "kind": "runtime",
  "threshold_owner": "BPTK-002",
  "state": "red",
  "gate": "active",
  "exclusion_reason": "Bring-your-own-file import is implemented: a user-owned file is threat- and protection-scanned, refused if flagged, then staged into a local content-addressed library with zero network transfer and marked never-published. The benchmark stays red because launching the staged entry needs the absent browser game runtime.",
  "prerequisite": [],
  "promotion_trigger": "Promote when the user file reaches launch locally with zero upload and the local-only entry stays unpublished.",
  "expected_failing_assertion": "No library staging exists.",
  "owner_item": "BPTK-114",
  "active_gate_evidence": "test/library.test.mjs asserts import stages the file locally with network_byte 0, marks it unpublished and launch-local, and writes the staged object.",
  "fixture": [
    "FIX-009"
  ],
  "procedure": [
    "Import the user file fixture and stage it locally.",
    "Assert zero network transfer of game byte.",
    "Launch from the library and assert the entry is not published."
  ],
  "pass_criterion": [
    "Launch is fully local.",
    "The entry is never published."
  ]
}
