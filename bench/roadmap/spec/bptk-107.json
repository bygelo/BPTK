{
  "benchmark_id": "BENCH-107",
  "item_id": "BPTK-107",
  "kind": "runtime",
  "threshold_owner": "BPTK-002",
  "state": "red",
  "gate": "active",
  "exclusion_reason": "The four host shapes are implemented: html, react, web component, and iframe all emit one content-addressed package identity, the web component releases every worker/audio/GPU handle on disconnect, and both framed shapes sandbox the embedded document. The benchmark stays red because counting leaked runtime handles across repeated live remounts needs the absent browser game runtime.",
  "prerequisite": [],
  "promotion_trigger": "Promote when the four host shape produce the identical artifact with zero remount leak and a sandboxed frame.",
  "expected_failing_assertion": "Only HTML and React host source exist and no leak proof exists.",
  "owner_item": "BPTK-107",
  "active_gate_evidence": "test/package.test.mjs asserts all four host shapes preserve one package identity, embed the id, the web component carries a disconnect release path, and the framed shapes are sandboxed.",
  "fixture": [
    "FIX-017"
  ],
  "procedure": [
    "Emit the package into each declared host shape.",
    "Compare runtime behavior for identity across host.",
    "Remount repeatedly and count leaked worker, audio, and GPU handle."
  ],
  "pass_criterion": [
    "All host shape behave identically.",
    "Remount leaks nothing and the frame is sandboxed."
  ]
}
