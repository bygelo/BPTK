{
  "benchmark_id": "BENCH-009",
  "item_id": "BPTK-009",
  "kind": "runtime",
  "threshold_owner": "BPTK-002",
  "state": "red",
  "gate": "active",
  "exclusion_reason": "Implemented but red because the frozen FIX-002 instruction, flag, FPU, exception, self-modification, and memory suite cannot be assembled while BPTK-002 owns no frozen denominator, and full instruction coverage does not exist. The bounded subset now covers the operand-size override (0x66): the 16-bit forms of the register push and pop decrement and restore the stack by two byte with the high word of the 32-bit register preserved, 16-bit arithmetic and immediate forms set the 16-bit carry, zero, sign, and overflow flags exactly, and the genuinely rare 16-bit control forms (ret16, call16, jmp16, pushf16, popf16, and the r/m16 call and jump) stay structured unsupported instead of mis-executing.",
  "prerequisite": [
    "BPTK-005",
    "BPTK-008"
  ],
  "promotion_trigger": "Promote when FIX-002 passes the pinned instruction, flag, FPU, exception, self-modification, and memory suite.",
  "expected_failing_assertion": "The complete i386 microprogram suite cannot execute to its frozen reference machine state while BPTK-002 owns no frozen denominator, and instruction outside the bounded subset stop as structured unsupported.",
  "owner_item": "BPTK-009",
  "active_gate_evidence": "Thirty-five runtime check exercise the real bptk run surface and now cover the widened bounded subset: the operand-size override with the high-word preservation rule, 16-bit carry-out semantics, the exact two-byte stack adjustment of a 16-bit push captured mid-run, the structured refusals of the rare 16-bit control forms, CPUID serving the declared processor identity leaf table with undeclared leaves stopping structured, plus the prior integer, flag, memory, rollback, and determinism coverage.",
  "fixture": [
    "FIX-002"
  ],
  "procedure": [
    "Run each microprogram with deterministic memory and instruction budget.",
    "Compare registers, flags, exception, FPU, memory, and trace hash."
  ],
  "pass_criterion": [
    "All required instruction cases match the reference state.",
    "Unsupported instruction produces a stable diagnostic rather than divergence."
  ]
}
