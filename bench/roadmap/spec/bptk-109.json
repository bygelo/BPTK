{
  "benchmark_id": "BENCH-109",
  "item_id": "BPTK-109",
  "kind": "runtime",
  "threshold_owner": "BPTK-002",
  "state": "red",
  "gate": "active",
  "exclusion_reason": "The hosting-constraint emission is implemented: every host emission carries the declared cross-origin header set with the expected isolation and the single-thread fallback statement. The benchmark remains red because measuring the served document on the declared host needs the hosting environment that is absent.",
  "prerequisite": [],
  "promotion_trigger": "Promote when the emitted constraint yields isolation on the declared host and the absent-isolation case selects the single-thread bundle.",
  "expected_failing_assertion": "No header emission exists.",
  "owner_item": "BPTK-109",
  "active_gate_evidence": "The corpus suite asserts the constraint fields on the real package host emission through the CLI.",
  "fixture": [
    "FIX-018"
  ],
  "procedure": [
    "Emit the constraint and measure crossOriginIsolated on the declared host.",
    "Serve without the constraint and assert the single-thread selection.",
    "Assert no shared-memory build is ever served unisolated."
  ],
  "pass_criterion": [
    "Constraint yields isolation.",
    "Absent isolation selects the single-thread bundle."
  ]
}
