{
  "benchmark_id": "BENCH-026",
  "item_id": "BPTK-026",
  "kind": "runtime",
  "threshold_owner": "BPTK-002",
  "state": "red",
  "gate": "active",
  "exclusion_reason": "Implemented but red because no browser WebRTC-data or WebSocket relay, no real consent surface, and no guest ws2_32 import binding exist, so the FIX-019 live transport case (real echo, latency, proxy, and offline over a browser-permitted transport) is unmet, and the prerequisite (BPTK-004 approval, BPTK-021, BPTK-024) remain red. The mediated network bridge exists (lib/net.mjs): the Winsock and DirectPlay contract over a consented, allowlisted in-memory relay that opens no host socket, with reliable and ordered stream, unreliable and unordered datagram, reconnect, lockstep determinism, name-resolution refusal, socket option, latency, and an offline failure case.",
  "prerequisite": ["BPTK-004", "BPTK-010", "BPTK-021", "BPTK-024"],
  "promotion_trigger": "Promote to passing when FIX-019 communicates through the declared browser transport with consent, endpoint allowlist, bounded latency, reconnect, denial, and offline behavior against a live relay.",
  "expected_failing_assertion": "The native network fixture cannot communicate through a browser-permitted transport, so the live FIX-019 echo, latency, proxy, and offline case has no relay to run against.",
  "owner_item": "BPTK-026",
  "active_gate_evidence": "The net conformance suite carries 22 case over 19 served ws2_32 export with zero uncovered export and zero failing case; the two-instance fixture (runTwoInstanceFixture, lib/net.mjs) establishes a DirectPlay session, exchanges an ordered reliable set that arrives in order and an unordered datagram set that matches the sent multiset, runs 16 lockstep turns whose determinism digest matches across instances, observes a 50 ms round-trip latency, discloses the offline case with the WSAENETDOWN contract and no fallback, and reports zero off-allowlist and zero un-consented connection with no host socket opened; name resolution refuses an off-allowlist or un-consented name with WSAHOST_NOT_FOUND and performs no lookup; test/policy.test.mjs proves the off policy denies and the prompt policy requires consent with zero network attempt.",
  "fixture": ["FIX-019"],
  "procedure": ["Run local echo, denied endpoint, reconnect, packet loss, latency, proxy, and offline cases.", "Capture consent, endpoint, transport, byte, order, latency, retry, and error artifact."],
  "pass_criterion": ["The allowed protocol scenario completes inside its declared semantic and latency tolerance.", "Denied, unavailable, and offline cases disclose the exact network boundary without fallback upload."]
}
