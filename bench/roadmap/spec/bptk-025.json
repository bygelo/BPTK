{
  "benchmark_id": "BENCH-025",
  "item_id": "BPTK-025",
  "kind": "runtime",
  "threshold_owner": "BPTK-002",
  "state": "red",
  "gate": "excluded",
  "exclusion_reason": "Live capability-based thread selection exists, but no worker-backed package, isolation-header deployment, or synchronization behavior exists.",
  "prerequisite": ["BPTK-021", "BPTK-024"],
  "promotion_trigger": "Promote when FIX-018 selects the correct build, verifies COOP and COEP, completes synchronization, and fails explicitly without isolation.",
  "expected_failing_assertion": "The thread fixture cannot share Wasm memory in a deployed BPTK package.",
  "owner_item": "BPTK-025",
  "active_gate_evidence": "The product observes isolation and SharedArrayBuffer live, writes selection configuration atomically, and blocks forced threading when unavailable.",
  "fixture": ["FIX-018"],
  "procedure": ["Run threaded and single-thread package under valid, missing, and conflicting header profile.", "Compare build selection, worker pool, synchronization, proxy, timing, and diagnostic artifact."],
  "pass_criterion": ["The isolated threaded case completes deterministically.", "Every non-isolated case chooses the declared alternate build or blocks with a precise reason."]
}
