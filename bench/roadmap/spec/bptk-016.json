{
  "benchmark_id": "BENCH-016",
  "item_id": "BPTK-016",
  "kind": "runtime",
  "threshold_owner": "BPTK-002",
  "state": "red",
  "gate": "excluded",
  "exclusion_reason": "Safe classification and asset-package diagnosis exist, but no staged importer or integrated synthetic PE32 2D first-playable runtime exists.",
  "prerequisite": ["BPTK-004", "BPTK-007", "BPTK-008", "BPTK-009", "BPTK-010", "BPTK-011", "BPTK-012", "BPTK-013", "BPTK-014", "BPTK-015"],
  "promotion_trigger": "Promote when FIX-009 valid packages import to the same content manifest, every malicious case rolls back safely, and FIX-027 completes the integrated interactive and reload scenario within BPTK-002 threshold.",
  "expected_failing_assertion": "No supported package can be staged and no single P1 scenario proves that the loader, CPU, Win32, graphics, audio, input, and storage component operate together.",
  "owner_item": "BPTK-016",
  "active_gate_evidence": "test/boundary.test.mjs proves a temporary asset package is neither staged nor executed; the planning catalog remains metadata only.",
  "fixture": ["FIX-009", "FIX-027"],
  "procedure": ["Import folder, ZIP, bundle, and selected installer cases under byte, entry, depth, path, and quota limits.", "Run the integrated 2D package through load, interaction, visual, audio, save, exit, reload, and state-recovery steps.", "Compare package manifest, runtime trace, image, audio, input, persisted state, rollback, and error result to the precommitted threshold."],
  "pass_criterion": ["Each valid package produces its golden manifest and every invalid or interrupted case leaves no partial state.", "FIX-027 reaches interactive, produces the expected visual and audio artifact, accepts deterministic input, persists state, and recovers it after reload within every BPTK-002 threshold."]
}
