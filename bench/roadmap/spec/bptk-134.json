{
  "benchmark_id": "BENCH-134",
  "item_id": "BPTK-134",
  "kind": "runtime",
  "threshold_owner": "BPTK-002",
  "state": "red",
  "gate": "active",
  "exclusion_reason": "The least-privilege capability manifest is synthesized by default on every packaged bundle, a declared grant or an out-of-policy capability is refused at the run surface, and the surfaced capability report distinguishes granted from denied. The benchmark remains red because the runtime serves no capability yet and the browser-side consent surface is absent.",
  "prerequisite": [
    "BPTK-089"
  ],
  "promotion_trigger": "Promote when undeclared use is denied with surfaced reason and the defaults hold on every lane.",
  "expected_failing_assertion": "No capability manifest enforcement exists.",
  "owner_item": "BPTK-134",
  "active_gate_evidence": "The security test refuses a granted network capability and an out-of-policy clipboard capability with containment_policy_violation, and the clean run reports is_least_privilege true with zero grant.",
  "fixture": [
    "FIX-017"
  ],
  "procedure": [
    "Run the fixture inside its manifest and assert success.",
    "Trigger each undeclared capability and assert denial with surfaced reason.",
    "Assert default network denial and storage scope."
  ],
  "pass_criterion": [
    "Undeclared use is denied and surfaced.",
    "Least-privilege defaults hold."
  ]
}
