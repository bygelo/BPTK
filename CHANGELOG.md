# Changelog

All notable planning and implementation change will be recorded here.

## Unreleased

### Added

- `WaitOnAddress` wakes when the other guest thread stores a new value at the wait cell (not only on `WakeByAddressSingle` / `WakeByAddressAll`). `CreateEventExW` maps `CREATE_EVENT_MANUAL_RESET` / `CREATE_EVENT_INITIAL_SET` onto the existing event table and refuses unknown flags 87. SuperTux 50M + data + `--datadir` (5134 host files): **instruction_budget_exhausted** after **50000000** instruction / **8967** HLE (~68.9 s) at `vcruntime140+0xef28`. Main still parks `WaitOnAddress` INFINITE; the worker `HeapAlloc`s and does not store that cell. Raised 80M: the worker finishes and wakes; `glTexImage2D` / `FindNextFileW` / zlib inflate; **80000000** / **20442** HLE at `zlib1+0x1303`. 150M: **150000000** / **25459** HLE (~410 s) at `zlib1+0x1350` (same scalar inflate loop). `CreateEventExW` is served (was GetProcAddress 127). Last `CreateWindowExW` `0x10024`; last `wglCreateContext` `0x5000c`. `SwapBuffers` 0 / `presentCount` 0. Inflating a PNG is not a presented frame. `passing` stays 1.
- Generic live Win32 input: a browser key or pointer updates the USER32 256-byte key table (`GetKeyboardState` / `GetKeyState` / `GetAsyncKeyState`) and posts `WM_KEYDOWN` / `WM_KEYUP` / button messages to the focused window. `injectKey` works with no window so SDL can poll state during init. `web/host.mjs` maps `KeyboardEvent.code` through `virtualKeyFromDomCode` (no title branch) and keeps the existing SDL queue for software titles. SuperTux is not remesured (wait sibling owns `SleepConditionVariableSRW`). `passing` stays 1.
- `SleepConditionVariableSRW` / `SleepConditionVariableCS` park on the guest-thread scheduler the way `WaitOnAddress` already does: an infinite wait switches to the other ready context instead of throwing `hle_wait_deadlock`. `WakeConditionVariable` / `WakeAllConditionVariable` resume matching waiters; a wake with no waiter is credited so it is not lost. NULL condition or lock refuse 87. SuperTux 50M + data + `--datadir` (5134 host files): **guest_exception `0x406d1388`** (MSVC `SetThreadName`) after **49907791** instruction / **8894** HLE. OpenAL `CreateThread` last `0x10011`; `InitOnceComplete` 1; SRW lock acquire/release after the wait. `CreateIconFromResource` `0x8208`. Last `CreateWindowExW` `0x10024`; last `wglCreateContext` `0x5000c`. `SwapBuffers` is never reached. A woken worker is not a presented frame. `passing` stays 1.
- The live session treats a current WGL context / `gdi32!SwapBuffers` front buffer as video: `hasVideo` and `presentCount` follow GL, not only an SDL software surface, and `composeGuestSurface` prefers that front buffer so the browser `putImageData` blit is the swapped color buffer. `SDL_GL_SwapBuffers` copies the same buffer. A recorded `glDrawArrays` is still not a frame. No title branch. SuperTux is not remesured here (current origin hole is `SleepConditionVariableSRW`). `passing` stays 1.
- i386 CPUID leaf `0x80000000` is in the declared processor table: EAX reports that leaf as the highest extended function, so a guest that probes for AMD extras or the brand string sees no further extended leaves. Undeclared leaves still stop. The 64-bit oracle and WASM tier answer the same leaf. SuperTux 50M + data + `--datadir` (5134 host files): **hle_wait_deadlock `kernel32!SleepConditionVariableSRW`** after **49691538** instruction / **8550** HLE. `CreateIconFromResource` `0x8208`; `InitOnceBeginInitialize` 1; `SHGetKnownFolderPath` S_OK; OpenAL `CreateThread` `0x10010` then `AcquireSRWLockExclusive`. Last `CreateWindowExW` `0x10024`; last `wglCreateContext` `0x5000c`. `SwapBuffers` is never reached. A CPUID max-extended answer is not a presented frame. `passing` stays 1.
- Generic `gdi32!SwapBuffers` (and `opengl32!wglSwapLayerBuffers`) copies the GL color buffer to a presented front buffer. A NULL DC refuses 6; a DC without the declared double-buffer format refuses 2000. `composeGuestSurface` / the live session blit that front buffer (browser `putImageData`), not a recorded `glDrawArrays`. No title branch. SuperTux is not remesured here (current origin hole is CPUID leaf `0x80000000`). `passing` stays 1.
- SuperTux past the known-folder IAT. `SHGetKnownFolderPath` writes the same declared virtual profile `SHGetFolderPath` already serves (Desktop / Documents / Roaming and Local AppData / ProgramData / Windows / System / Program Files / Profile), allocated with `CoTaskMemAlloc` so the caller `CoTaskMemFree`s the PWSTR. NULL GUID or dest refuse `E_INVALIDARG` 87; an unknown GUID is the same. SuperTux 50M + data + `--datadir` (5134 host files): **unsupported_opcode CPUID leaf `0x80000000`** at `openal32+0xb1e79` after **49675227** instruction / **8464** HLE. `SHGetKnownFolderPath` S_OK; `CoTaskMemFree` ran; OpenAL `CreateFileW` of `C:\Users\Guest\AppData\Roaming\alsoft.ini`. `CreateIconFromResource` `0x8208`; `InitOnceBeginInitialize` 1. `SetFilePointerEx` 1; console.err empty; last `CreateWindowExW` `0x10024`; last `wglCreateContext` `0x5000c`. `SwapBuffers` is never reached. A known-folder path is not a presented frame. `passing` stays 1.
- SuperTux past OpenAL one-time init. `InitOnceBeginInitialize` / `InitOnceComplete` are generic kernel32: NULL once or pending dest refuse 87, unknown flags 87, CHECK_ONLY on an uninitialized cell returns 0, a first Begin sets pending and returns 1, Complete stores an aligned context with the complete bit, and a later Begin reports pending false. SuperTux 50M + data + `--datadir` (5134 host files): **fetch_fault `shell32!SHGetKnownFolderPath`** after **49667853** instruction / **8406** HLE. `InitOnceBeginInitialize` 1; `CreateIconFromResource` `0x8208`. Previous EIP `openal32+0xda5bb` (`jmp [0x280e63bc]`; IAT still held hint RVA `0x16174e`). `SetFilePointerEx` 1; console.err empty; last `CreateWindowExW` `0x10024`; last `wglCreateContext` `0x5000c`. `SwapBuffers` is never reached. A pending InitOnce is not a presented frame. `passing` stays 1.
- SuperTux past the window-icon IAT. `CreateIconFromResource` / `CreateIconFromResourceEx` allocate from the same `0x8200+4n` pool as `CreateIconIndirect`: a live `HICON` from RT_ICON DIB bits (version `0x30000`), without decoding pixels. NULL bits, zero size, or a wrong version refuse 87. SuperTux 50M + data + `--datadir` (5134 host files): **fetch_fault `kernel32!InitOnceBeginInitialize`** after **49661659** instruction / **8383** HLE. `CreateIconFromResource` `0x8208`; two `SendMessageW` (WM_SETICON). Previous EIP `openal32+0x5fbb2` (`call [0x280e60d4]`; IAT still held hint RVA `0x1647ec`). `SetFilePointerEx` 1; console.err empty; last `CreateWindowExW` `0x10024`; last `wglCreateContext` `0x5000c`. `SwapBuffers` is never reached. A window icon handle is not a presented frame. `passing` stays 1.
- i386 PAVGB (`0F E0` / `66 0F E0`) is the rounded unsigned-byte average `(a+b+1)>>1` in each MMX or XMM lane. SuperTux 50M + data + `--datadir` (5134 host files): **fetch_fault `user32!CreateIconFromResource`** after **49660809** instruction / **8371** HLE. Previous EIP `sdl2` `0x2c0d4f25` (`call [0x2c0f82a8]`). `SetFilePointerEx` 1; console.err empty; last `CreateWindowExW` `0x10024`; last `wglCreateContext` `0x5000c`. The libpng filter around `+0x2080b` had no further unserved packed op (PADDB / PSUBB / PMINSW already served). `SwapBuffers` is never reached. A decoded icon PNG is not a presented frame. `passing` stays 1.
- i386 PMINSW (`0F EA` / `66 0F EA`) keeps the smaller signed word in each MMX or XMM lane. SuperTux 50M + data + `--datadir` (5134 host files): **unsupported_opcode `0x0f 0xe0` PAVGB** at `libpng16+0x2080b` after **42080197** instruction / **8320** HLE. `SetFilePointerEx` 1; console.err empty; last `wglCreateContext` `0x5000c`. `SwapBuffers` is never reached. Inflating a PNG is not a presented frame. `passing` stays 1.
- i386 BSWAP (`0F C8`–`CF`) reverses a 32-bit register with no flag effect (the 16-bit form reverses AX and leaves the high word, matching the 64-bit oracle). Encoding is two-byte with no ModRM so the decode sweep stays aligned. SuperTux 50M + data + `--datadir` (5134 host files): **unsupported_opcode `0x0f 0xea` PMINSW** at `libpng16+0x20ad4` after **42020032** instruction / **8320** HLE. `SetFilePointerEx` 1; console.err empty; last `wglCreateContext` `0x5000c`. `SwapBuffers` is never reached. Inflating a PNG is not a presented frame. `passing` stays 1.
- SuperTux past the GL 1.1 version reject and the icon-seek BOOL. `glGetString(GL_VERSION)` is `"2.1 BPTK"` so a guest that sscanf's the major does not throw on 1.x; `GL_EXTENSIONS` declares `GL_ARB_texture_non_power_of_two` (NPOT stores already). `SetFilePointerEx` is BOOL (1 on success, 0 on negative seek 131); `SetFilePointer` is the 32-bit twin. SuperTux 50M + data + `--datadir` (5134 host files): **unsupported_opcode `0x0f 0xc8` BSWAP** at `zlib1+0x81c4` after **40690494** instruction / **8282** HLE. `SetFilePointerEx` 1; console.err empty (past `Can't seek` on the window icon). `SwapBuffers` is never reached. Inflating a PNG is not a presented frame. `passing` stays 1.
- SuperTux past the WGL ARB extension-string miss. `wglGetExtensionsStringARB` returns one declared token (`WGL_ARB_extensions_string`) on a live DC; hdc 0 refuses 6. SuperTux 50M + data + `--datadir` (5134 host files): **process_exit 1** after **41408366** instruction / **19589** HLE. `wglGetExtensionsStringARB` hits; `wglGetProcAddress` misses are empty; `glGetString` VERSION/EXTENSIONS and `glColor4ub` run. Dummy context still deleted after the query. `SwapBuffers` is never reached. A WGL extension string is not a presented frame. `passing` stays 1.
- SuperTux past the GL 1.1 GetProcAddress miss. `glBegin` / `glEnd` / `glVertex2f` / `glVertex3fv` / `glColor3fv` / `glColor4ub` / `glTexCoord2f` / `glRectf` / `glRotatef` / `glDepthFunc` / `glLineWidth` / `glPointSize` / `glShadeModel` / `glTexEnvf` / `glTexSubImage2D` / `glDrawPixels` / `glRasterPos2i` / `glReadBuffer` / `glGetFloatv` / `glGetPointerv` / `glBlendEquation` / `glBlendFuncSeparate` are generic opengl32 exports (immediate-mode is recorded, not a presented frame). SuperTux 50M + data + `--datadir` (5134 host files): **process_exit 1** after **41406979** instruction / **19588** HLE. `ChoosePixelFormat` 1; `SetPixelFormat` 1; last `wglCreateContext` `0x5000c`; `wglMakeCurrent` 1; SDL `wglGetProcAddress` of the GL 1.1 family hits; `glGetString` / `glGetIntegerv` / `glColor4ub` run. Dummy context still deleted after `wglGetExtensionsStringARB` 0. `SwapBuffers` is never reached. A recorded `glBegin` is not a presented frame. `passing` stays 1.
- SuperTux past the pixel-format IAT. `ChoosePixelFormat` / `SetPixelFormat` / `GetPixelFormat` / `DescribePixelFormat` are one declared OpenGL RGBA format (index 1, 32/24/8, double-buffer); a NULL PFD refuses 87, an unknown index or a second SetPixelFormat 2000. SuperTux 50M + data + `--datadir` (5134 host files): **process_exit 1** after **41716001** instruction / **19906** HLE. `ChoosePixelFormat` 1; `SetPixelFormat` 1; `wglCreateContext` `0x50010`; `wglMakeCurrent` 1; `DescribePixelFormat` 1; then `wglDeleteContext` 1 after GetProcAddress of unserved GL 1.1 (`glBegin` / `glEnd` / `glVertex2f` / …) 127. `SwapBuffers` is never reached. A WGL context is not a presented frame. `passing` stays 1.
- SuperTux past the WGL GetProcAddress miss. `wglGetProcAddress` / `wglCreateContext` / `wglDeleteContext` / `wglMakeCurrent` / `wglShareLists` / `wglGetCurrentDC` / `wglGetCurrentContext` are generic opengl32 exports: a live DC issues an HGLRC, make-current binds it, and GetProcAddress of a served `gl*` / `wgl*` name returns the thunk. SuperTux 50M + data + `--datadir` (5134 host files): **fetch_fault `gdi32!ChoosePixelFormat`** after **39497908** instruction / **7947** HLE. `LoadLibraryW("OPENGL32.DLL")` returned `0x2000c`; GetProcAddress of the five SDL WGL names succeeded; last `CreateWindowExW` `0x10018`; `GetDC` `0x40024`. Previous EIP `sdl2` `0x2c0d1def` (`call [0x2c0f803c]`). A shown HWND is not a presented frame. `passing` stays 1.
- SuperTux past the OpenGL LoadLibrary miss. `opengl32.dll` is in the generic module table (handle `0x2000c`), so `LoadLibraryW("OPENGL32.DLL")` returns a handle `GetProcAddress` can resolve — same pattern as `ws2_32` / `comctl32`, no title branch. SuperTux 50M + data + `--datadir` (5134 host files): **process_exit 1** after **41500008** instruction / **19608** HLE. `LoadLibraryW("OPENGL32.DLL")` returned `0x2000c`; `GetProcAddress` of `wglGetProcAddress` / `wglCreateContext` / `wglMakeCurrent` / `wglDeleteContext` / `wglShareLists` is 0 / last_error 127, then `ExitProcess(1)`. `ChoosePixelFormat` is still never reached. A shown HWND is not a presented frame. `passing` stays 1.
- SuperTux past the class-info IAT. `GetClassInfoExW`/`A` and `GetClassInfoW`/`A` write a registered class (style, WndProc, extras, instance, icon, cursor, brush) into `WNDCLASSEX`/`WNDCLASS`; a NULL dest refuses 87 and an unknown name 1411. SuperTux 50M + data + `--datadir` (5134 host files): **process_exit 1** after **41435616** instruction / **19524** HLE. `GetClassInfoExW` returned 1; `ShowWindow` 1 (was visible); last `CreateWindowExW` `0x1001c`. SDL `LoadLibraryW("OPENGL32.DLL")` is 0 / last_error 126, then `ExitProcess(1)`. A shown HWND is not a presented frame. `passing` stays 1.
- SuperTux past the post-ShowWindow x87 pair. `FLD ST(i)` (`D9 C0+i`) pushes a copy of a live slot; `FSTP m80` / `FLD m80` convert the 64-bit stack value through the 80-bit memory format (double identity, not 80-bit arithmetic). SuperTux 50M + data + `--datadir` (5134 host files): **fetch_fault `user32!GetClassInfoExW`** after **40003291** instruction / **10088** HLE. `ShowWindow` returned 1 (was visible); last `CreateWindowExW` `0x1001c`. Previous EIP `sdl2` `0x2c0c9455` (`call [0x2c0f8330]`). A shown HWND is not a presented frame. `passing` stays 1.
- SuperTux past the drop-accept IAT. `DragAcceptFiles` records whether a HWND accepts dropped files; an invalid HWND refuses. No host drop target, so `WM_DROPFILES` is never posted. SuperTux 50M + data + `--datadir` (5134 host files): **unsupported_opcode x87 `FLD ST(0)` (`0xd9 0xc0`)** after **39550744** instruction / **8099** HLE. `DragAcceptFiles` returned 0; `ShowWindow` 0 (was hidden); last `CreateWindowExW` `0x1001c`. EIP `sdl2` `0x2c0f691a`. A shown HWND is not a presented frame. `passing` stays 1.
- SuperTux past the window-caption IAT. `SetWindowTextW`/`A` store the caption `GetWindowTextW`/`A` and `GetWindowTextLengthW`/`A` then report; an invalid HWND refuses. SuperTux 50M + data + `--datadir` (5134 host files): **fetch_fault `shell32!DragAcceptFiles`** after **39455186** instruction / **7853** HLE. `SetWindowTextW` returned 1. Previous EIP `sdl2` `0x2c0d3f27` (`call [0x2c0f8214]`). A HWND is not a presented frame. `passing` stays 1.
- SuperTux past the ICM-profile IAT. `GetICMProfileW`/`A` write one declared sRGB path for the virtual display (`C:\Windows\System32\spool\drivers\color\sRGB Color Space Profile.icm`); a NULL size pointer refuses 87, a short buffer 122. SuperTux 50M + data + `--datadir` (5134 host files): **fetch_fault `user32!SetWindowTextW`** after **39455072** instruction / **7851** HLE. `GetICMProfileW` returned 1; `SetWindowPos` 1; `DeleteDC` 1. Previous EIP `sdl2` `0x2c0d5222` (`call [0x2c0f8284]`). A HWND is not a presented frame. `passing` stays 1.
- SuperTux past the client-to-screen IAT. `ClientToScreen` / `ScreenToClient` add or subtract the window origin (no nonclient frame, so the client origin is the window origin); a NULL POINT refuses 87. SuperTux 50M + data + `--datadir` (5134 host files): **fetch_fault `gdi32!GetICMProfileW`** after **39451090** instruction / **7840** HLE. `ClientToScreen` returned 1; `GetMonitorInfoW` 1; `CreateDCW` `0x40020`. Previous EIP `sdl2` `0x2c0d3de8` (`call [0x2c0f8058]`). A HWND is not a presented frame. `passing` stays 1.
- SuperTux past the window-property IAT. `SetPropW`/`GetPropW`/`RemovePropW` (and the A twins) are one per-window HANDLE table keyed by name or atom; a NULL name refuses 87. SuperTux 50M + data + `--datadir` (5134 host files): **fetch_fault `user32!ClientToScreen`** after **39450685** instruction / **7837** HLE. `SetPropW` returned 1; `GetClientRect` 1. Previous EIP `sdl2` `0x2c0d3d66` (`call [0x2c0f83cc]`). A HWND is not a presented frame. `passing` stays 1.
- SuperTux past the window-long IAT. `GetWindowLongW` / `SetWindowLongW` are the `GetWindowLongA` twins; `GWL_HINSTANCE` (`-6`) is the `hInstance` `CreateWindowEx` stored. SuperTux 50M + data + `--datadir` (5134 host files): **fetch_fault `user32!SetPropW`** after **39450640** instruction / **7834** HLE. `GetWindowLongW` returned `0x400000`; `CreateWindowExW` `0x10014`. Previous EIP `sdl2` `0x2c0d3c21` (`call [0x2c0f827c]`). A HWND is not a presented frame. `passing` stays 1.
- SuperTux past the window-frame IAT and **creates a HWND**. `AdjustWindowRectEx` is the `AdjustWindowRect` twin: this HLE has no nonclient frame, so a valid RECT is left unchanged and a NULL dest refuses 87. SuperTux 50M + data + `--datadir` (5134 host files): **fetch_fault `user32!GetWindowLongW`** after **39445578** instruction / **7829** HLE. `AdjustWindowRectEx` returned 1; `RegisterClassExW` `0xC001`; `CreateWindowExW` `0x10014`; `GetDC` `0x4001c`. Previous EIP `sdl2` `0x2c0d3b5a` (`call [0x2c0f82c8]`). A HWND is not a presented frame. `passing` stays 1.
- SuperTux past the ToUnicode IAT. `ToUnicode` is the US 101 layout over the 256-byte key table: space and letters honor Shift/Caps, Ctrl/Alt produce no character, a NULL dest or non-positive count refuses 87. SuperTux 50M + data + `--datadir` (5134 host files): **fetch_fault `user32!AdjustWindowRectEx`** after **39445309** instruction / **7822** HLE. `ToUnicode` returned 1; last HLE `GetMonitorInfoW` / `MulDiv` 1280 and 800. Previous EIP `sdl2` `0x2c0d4136` (`call [0x2c0f82e8]`). Not a frame. `passing` stays 1.
- SuperTux past the keyboard-state IAT. `GetKeyboardState` writes 256 zero bytes (every key up, matching `GetKeyState`) and returns 1; a NULL dest refuses 87. SuperTux 50M + data + `--datadir` (5134 host files): **fetch_fault `user32!ToUnicode`** after **31884931** instruction / **6007** HLE. `GetKeyboardState` returned 1; `MapVirtualKeyW` 57 (space scan). Previous EIP `sdl2` `0x2c0ce031` (`call ebx` after `mov ebx, [0x2c0f8394]`). Not a frame. `passing` stays 1.
- SuperTux past the display-sleep IAT. `SetThreadExecutionState` records `ES_CONTINUOUS` (and `ES_SYSTEM_REQUIRED` / `ES_DISPLAY_REQUIRED`) and returns the previous state; a one-shot is accepted and forgotten. Zero, unknown bits, `ES_USER_PRESENT|ES_CONTINUOUS`, and `ES_AWAYMODE` without `ES_CONTINUOUS` refuse 87. The host never sleeps. SuperTux 50M + data + `--datadir` (5134 host files): **fetch_fault `user32!GetKeyboardState`** after **31884911** instruction / **6005** HLE. `SetThreadExecutionState` returned `0x80000000`; `CreateDIBSection` `0x4000c`; `CreateDCW` `0x40000`; `GetDC` `0x40008`. Previous EIP `sdl2` `0x2c0cdffa`. Not a frame. `passing` stays 1.
- SuperTux past the DIB and cursor IAT wall. `CreateDIBSection` allocates guest-visible bits (`VirtualAlloc`, BI_RGB/BI_BITFIELDS 24/32, no `hSection`). `CreateBitmap` serves 1/24/32 bpp. `CreateIconIndirect` / `CopyImage` / `DestroyIcon` track icon handles; `CopyImage` accepts a requested cursor size. `SystemParametersInfoW`/`A` writes `SPI_GETMOUSESPEED` (10), `SPI_GETMOUSE` (6,10,1), work area, wheel lines, and double-click time; unknown actions refuse 87. `RegisterWindowMessageA`/`W` allocates `0xC000+`. i386 `0F 5B` is `CVTPS2DQ` / `CVTTPS2DQ` / `CVTPD2DQ`. SuperTux 50M + data + `--datadir`: **fetch_fault `kernel32!SetThreadExecutionState`** after **31878842** instruction / **5994** HLE. `CreateDIBSection` returned `0x4000c`; `CreateBitmap` `0x40010`; `CreateIconIndirect` `0x8200`; `CopyImage` `0x8204`; both `SystemParametersInfoW` calls returned 1; `RegisterWindowMessageA` `0xC000`. Not a frame. `passing` stays 1.
- SuperTux past the SDL display/keyboard IAT wall. `SetConsoleCtrlHandler` records handlers and never fires them. `GetDoubleClickTime` is 500. `EnumResourceNamesW` walks the PE type→name directory and re-enters `ENUMRESNAMEPROC` when names exist (SuperTux has no `RT_GROUP_ICON`). One virtual desktop: `EnumDisplayMonitors` / `GetMonitorInfo` / `EnumDisplaySettings` / `EnumDisplayDevices` / `CreateDCW("DISPLAY"|`\\.\DISPLAY1`)` at 1920×1080. `MapVirtualKeyW` is US set-1; `GetKeyState` is up/untoggled; `LoadCursorW` is the `LoadCursorA` twin. SuperTux 50M + data + `--datadir`: **fetch_fault `gdi32!CreateDIBSection`** after **31870988** instruction / **5963** HLE. `CreateDCW` returned `0x40000`; `GetDeviceCaps` 1920/1080; `GetDIBits` ran; `LoadCursorW` returned a handle; `GetDC` returned `0x40008`. Not a frame. `passing` stays 1.
- Counted `MultiByteToWideChar` writes UTF-16 without a forced NUL when dest count equals the character count (Win32 does not terminate a counted conversion); extra dest room still gets a NUL so CRT `wcslen` works. `LCMapStringEx` treats a 32-bit-masked cchDest of 0 as a size query and refuses a NULL dest with a non-zero count instead of writing address 0. `CreateFile2` reads `dwFileFlags` at extra+8. `GetFileInformationByHandleEx` serves FileBasicInfo (class 0) and FileStandardInfo (class 1). `FileTimeToSystemTime` / `SystemTimeToFileTime` convert 100 ns since 1601; `SystemTimeToTzSpecificLocalTime` is an identity copy (GetTimeZoneInformation is UTC). `DeleteFileW` / `RemoveDirectoryA`/`W` are the twins of the existing A/create-directory surface. SuperTux 50M with `--datadir C:\game\data` and the real portable tree (5134 host files): physfs IAT is **0 unserved**; `CreateFileW("C:\game\data")` succeeds (handle 65543); then **guest_exception `0xe06d7363`** at **31435057** instruction / **4362** HLE after honest FILE_NOT_FOUND on `…\supertux2\config` and `C:\game\data\config`. SuperTux `ConfigSubsystem` catches that `runtime_error` on real Windows; our RaiseException has no guest C++ catch frame. Not a frame. `passing` stays 1.
- CreateFile / GetFullPathName now resolve cwd-relative paths and `.` / `..` (escape past `c:\` is still 87). `CreateFileW("")` is PATH_NOT_FOUND 3, not INVALID_PARAMETER 87. OPEN_EXISTING + `FILE_FLAG_BACKUP_SEMANTICS` opens a declared directory (`c:\`, `c:\game`, a `virtualDir`, or a host-file prefix). `GetFileInformationByHandle` reports `FILE_ATTRIBUTE_DIRECTORY` on those handles. SuperTux 50M still **guest_exception `0xe06d7363`** at **31359471** instruction / **3944** HLE — MSVC `canonical` CreateFileW of an empty path (last_error 3). `--datadir C:\game\data` plus the real portable tree (5134 host files) does not change that empty CreateFile. The 10M product cap still lands in OpenAL. Not a frame. `passing` stays 1.
- Sidecar IAT holes were jumping to hint RVAs. `SHGetFolderPathW`/`A` write a declared virtual profile (`C:\Users\Guest\…`); `OpenProcessToken` issues a closeable token for `GetCurrentProcess`; `RegisterClassExA` / `UnregisterClassA` are the ANSI twins. A fetch_fault now records `previous_eip`. SuperTux 50M: SDL2 `SHGetFolderPathW(CSIDL_APPDATA|CREATE)` returns `S_OK`, physfs `OpenProcessToken` succeeds, then **guest_exception `0xe06d7363`** at **31359444** instruction / **3944** HLE / 24.0 s (`vcruntime140`). The 10M product cap still lands in OpenAL. Not a frame. `passing` stays 1.
- i386 PINSRW (`0F C4` / `66 0F C4`) inserts a GPR or memory word into the selected MMX/XMM lane. SuperTux leaves OpenAL table init at **30017849** instruction, then `ucrtbase` PINSRW, then CRT argv (`GetCommandLineW` / `CommandLineToArgvW`). A 50M remesure stops `fetch_fault` at `0x1397bc` (31124264 instruction, 3061 HLE, 32.0 s) — that address is the image RVA of `.text` `0x5397bc`, not a mapped VA. The 10M product cap still lands in OpenAL. Not a frame. `passing` stays 1.
- Honest ANSI / resource / dialog HLE so PuTTYgen leaves the import wall. `FindResourceA` walks a generic PE resource tree; `CreateDialogParamA` / `DialogBoxParamA` instantiate `RT_DIALOG` and re-enter the guest `DLGPROC` for `WM_INITDIALOG`. comdlg32 `GetOpenFileNameA` / `GetSaveFileNameA` return 0 (cancel). MessageBox non-`MB_OK` returns 0. A modal dialog that never calls `EndDialog` stops as `hle_dialog_modal_idle`, not a fake `IDOK`. CORPUS-011 PuTTYgen is **174/174** IAT-bound and reaches `DialogBoxParamA` (template 201) then **10M / 1233 HLE / 8.8 s** still inside the guest `WM_INITDIALOG` (`CreateWindowExA` 34, `MapDialogRect` 35, `AppendMenuA` 36). Not a shown window. OpenTTD stays 161/302. SuperTux OpenAL numbers are unchanged. `passing` stays 1.
- i386 probe caches register-only INC/DEC, Jcc rel8, LAHF, TEST AH, and the SSE scalar/move forms the OpenAL table-init loop retires. SuperTux 10M OpenAL is **9.46 s / 1.06M insn/s** (was 14.2 s) at the same `0x280ab69f`; 25M is **20.1 s / 1.25M insn/s** at `0x280abeb1`. Still not a frame. Plink `--version` stays `process_exit` 0 (766759 instruction). `glDrawArrays` is still not a playable frame.
- i386 SSE register-register paths no longer allocate a Buffer per MOVAPS/ADDSD/MULSD/DIVSD/CVTDQ2PD. SuperTux 10M OpenAL is **14.2 s / 705k insn/s** (was 33.4 s) at the same `0x280ab69f`; 25M is 33.0 s at `0x280abeb1`. Still not a frame. Plink i386 `--version` is `process_exit` 0 (`plink: Release 0.74`, 766759 instruction). `glDrawArrays` is still not a playable frame.
- CRT data imports (`_iob`, `_tzname`, `__mb_cur_max`, `_environ`, `__winitenv`) bind to the live FILE table and cells, not a CALL thunk. i386 `_initterm` walks each guest constructor (the x64 twin). FLD1 is the real `D9 E8` and pushes on an empty stack; `FSTP ST(i)` copies then pops. jq `--version` is `process_exit` 0 (`jq-1.7.1\n`, 4648 instruction, 110 HLE). jq `-n 1` is `process_exit` 0 after 4361398 instruction with the colored `1`. SuperTux is unchanged: 10M OpenAL budget stop. `glDrawArrays` is still not a playable frame.
- i386 probe reuses the per-instruction undo/trace buffers and caches the last mapped section so a sidecar-heavy run does not walk 21 PE section tables on every fetch. The HLE thunk page is writable (delay-load / optional IAT stores). i386 CRT argv is a 4-byte pointer vector; `__getmainargs` / `__wgetmainargs` write the real argc. Ripgrep `--version` stays `process_exit` 0 (181697 instruction). SuperTux 10M still stops in OpenAL table init at `0x280ab69f` (1045 HLE, 33.4 s). jq i386 moves past the thunk write_fault to `process_exit` 2; `fputc` is still EOF because `_iob` is bound as a code thunk, not the FILE table. `glDrawArrays` is still not a playable frame.
- i386 `PMOVMSKB` reads the r/m xmm (hashbrown's `pmovmskb edi, xmm0` was reading xmm7). `MapViewOfFile` now returns a granularity-aligned committed VAD so `VirtualProtect`/`UnmapViewOfFile` succeed. i386 `executeProbe` forwards `host_file`. Ripgrep search of `PCRE2` in the staged README is `process_exit` 0 after 1701493 instruction and 8219 HLE, with the real line-numbered hits. `--version` is `process_exit` 0 (181697 instruction). SuperTux is unchanged: 10M OpenAL budget stop. `glDrawArrays` is still not a playable frame.
- PEB.ProcessParameters (`+0x10`) now points at a real parameters block in the PEB page so CRT teardown can read Flags at `+8`. Ripgrep `--version` is `process_exit` 0 after printing `ripgrep 14.1.1` (181840 instruction, 458 HLE); no-arg is `process_exit` 2 with `rg: ripgrep requires at least one pattern to execute a search` (176792 instruction). SuperTux is unchanged: 10M budget exhausted in OpenAL table init. `glDrawArrays` is still not a playable frame.
- `WriteConsoleW` now writes the character count (not the UTF-16 byte count) to `lpNumberOfCharsWritten`; rust treated the doubled count as a short write and re-panicked until the 1MB stack died. The i386 probe forwards `command_line` into the HLE. Ripgrep `--version` prints `ripgrep 14.1.1` plus the PCRE2 line (181688 instruction, 454 HLE) and no-arg prints `rg: ripgrep requires at least one pattern to execute a search` (176640 instruction); both then `read_fault` at `[eax+8]` with eax 0 during CRT teardown (`0x6f83d6`). SuperTux is unchanged: 10M budget exhausted in OpenAL table init. `glDrawArrays` is still not a playable frame.
- i386 x87 `FSTENV`/`FLDENV` (`D9 /6` `/4`) write and restore the 32-bit protected-mode environment; `GetModuleFileName(NULL)` is the current image path; `fs:[0x2C]` is a live PE TLS array (the i386 twin of x64 `gs:[0x58]`); `SHUFPS`/`SHUFPD` (`0F C6`) shuffle packed lanes; `GetConsoleMode` is the two-argument BOOL contract and writes the mode dword (the one-argument stdcall pop was leaving the pointer on the stack). SuperTux win32 stays **722/722**, runs past ucrt `FSTENV`, and exhausts the 10M probe cap (1045 HLE) and a 25M probe (1047 HLE) inside a finite OpenAL table-init series at `openal32` `0x280ab69f` / `0x280abeb1`. Ripgrep moved **20999 → 214704** instruction and 185 → 889 HLE calls, reached `WriteConsoleW`, then `read_fault` at `0x6FFFF000` (a stack probe below the mapped stack). `glDrawArrays` is still not a playable frame.
- SuperTux's 32k locale `read_fault` was `WideCharToMultiByte(CP_ACP=0)` refused as invalid: conversion and `GetCPInfo` now resolve CP_ACP / CP_OEMCP / CP_THREAD_ACP to the declared 1252 page (`IsValidCodePage(0)` still returns 0). HeapAlloc rounds size up to 8 bytes. Vista locale-name / AppPolicy / `FlsGetValue2` / `GetSystemTimePreciseAsFileTime` / `GetTempPath2W` bind; `EnumSystemLocalesEx` still refuses 120. i386 gained CVTDQ2PD/CVTDQ2PS, STMXCSR/LDMXCSR, PEXTRW, UNPCKLP*/UNPCKHP*, and x87 FRNDINT. SuperTux win32 stays **722/722**, executes **643190** instruction and 1043 HLE calls through DllMain, locale, OpenAL CRT math, then `unsupported_opcode` x87 `FSTENV` (`D9 /6`) at ucrt `0x3505054e`. Ripgrep moved **20790 → 20999** instruction and still `read_fault` at address 0. `glDrawArrays` is still not a playable frame.
- SuperTux's 4261-insn `0xe06d7363` was `std::bad_alloc`, not missing C++ EH: ucrt `HeapAlloc(NULL)` before `HeapCreate` now uses the process heap, and `FlsGet/SetValue(FLS_OUT_OF_INDEXES)` keeps the CRT per-thread block. i386 `0x8C`/`0x8E` MOV Sreg zero-extends the Win32 selectors. `VirtualProtect` succeeds on a mapped image or sidecar range so ucrt can encode a delay-load slot. `LoadLibrary` appends `.dll`, honors `LOAD_LIBRARY_SEARCH_SYSTEM32`, and resolves api-set / `kernelbase` names to kernel32. `VerSetConditionMask` / `VerifyVersionInfoW` / `IsThreadAFiber` bind the ucrt IAT that was jumping to RVA `0x105860`. Counted `LCMapStringW` no longer invents a terminator (return 5 for five characters); `LCMapStringEx`, `LocaleNameToLCID`, and `IsValidLocaleName` serve the Vista GetProcAddress path. SuperTux win32 stays **722/722**, executes **32245** instruction and 508 HLE calls through DllMain and locale init, then `read_fault` at `0xD81D1B4C` (ucrt `0x3502d9a3`) after `GetCPInfo`. Ripgrep moved **20467 → 20790** instruction and still `read_fault` at address 0. `glDrawArrays` is still not a playable frame.
- SuperTux now runs real sidecar DllMain. i386 CRT/SDL HLE is cdecl so `_initterm` RET no longer pops the NX stack (the 1498-insn fetch was ABI, not executable-stack). `GetModuleHandle(NULL)` returns the load base (Win32 HMODULE contract). Package-local exports win over an HLE row so shipped ucrt `_initterm` walks constructors. Sidecar TLS then DllMain(DLL_PROCESS_ATTACH) fire dependency-first; sidecar modules are registered for GetProcAddress. `DisableThreadLibraryCalls` binds the leftover IAT that was jumping to RVA 0x22588. EnterCriticalSection adopts a zeroed CRITICAL_SECTION on first use. SuperTux win32 stays **722/722**, executes **4261** instruction and 99 HLE calls through CRT/DllMain, then `guest_exception` 0xe06d7363 (MSVC C++ EH) inside vcruntime140. Ripgrep is unchanged: **20467** instruction then `read_fault` at address 0. `glDrawArrays` is still not a playable frame.
- Closed SuperTux's remaining IAT and served the SSE half-register moves its CRT used at instruction 19. Bounded OpenGL 1.1 HLE (`lib/gl.mjs`) covers the 29 `opengl32` imports (real color buffer, texture names, `glClear`/`glReadPixels`/`glTexImage2D`/`glGetString`; `glDrawArrays` records a draw and does not paint a playable frame). Leftover kernel32 file/locale rows, `shell32!ShellExecuteA`, and dbghelp walk/symbol rows are served or honestly refused. `GetTickCount64` writes EDX for the i386 64-bit return. i386 `0x0F 12/13/16/17` is MOVLPS/MOVHPS (and the 0x66 MOVLPD/MOVHPD twin); store encodings are memory-only `#UD`. SuperTux win32 is now **722/722** served, executes **1498** instruction and 9 HLE CRT calls from entry, then `fetch_fault` ("Stack memory is not executable"). Ripgrep is unchanged: **20467** instruction then `read_fault` at address 0. DllMain is still not run.
- Mapped package-local DLLs as real PE modules (`lib/sidecar.mjs`) and bound the guest IAT to parsed export VAs, including api-set CRT names that miss the HLE forwarding to a shipped `ucrtbase.dll`. A synthetic `helper.dll!add_one` call returns through the sidecar image (eax 6 → 7). SuperTux win32 moved from 190/722 to 668/722 on sidecar plus ucrtbase alias before the OpenGL/leftover-HLE close. DllMain is still not run.
- Served i386 CMPXCHG8B (`0x0F C7 /1` m64, LOCK is identity, register form `#UD`). Ripgrep 14.1.1 i686 no longer stops on unsupported `0x0f c7`; it executes **20467** instruction from entry and then `read_fault` at address 0.
- Grew the lawful freeware corpus 11 → 14 (curl 8.22.0 win64, ripgrep 14.1.1 i686, SuperTux 0.7.0 win32 portable) with pinned sha256 and payloads still outside git. Split ingest's whole-archive cap from `chunk_input_byte` into `archive_input_byte` (512 MiB, matching the download bound) so a 307 MiB game zip is not mistaken for one inflate. Inspect and security file-size limits match. Archive ingest now classifies the selected executable's machine, so curl is `x86_64` rather than a false i386.
- Forwarded Windows api-set and ntdll import names to the existing kernel32/msvcrt HLE rows in `computeImportService` and `lookupExport`, so `api-ms-win-core-synch!WaitOnAddress` is the same real wait already served on kernel32.
- Widened the Win32 core HLE with the path/console/sync/ntdll/userenv/bcryptprimitives surface a Rust/MSVC console program and a C++ SDL game both import: wide siblings of already-served ANSI rows, current-directory and full-path, console buffer/attribute, `CompareStringOrdinal`, proc-thread attribute list, file-information-by-handle, overlapped Read/WriteFileEx as the sync sibling, SRW locks and condition variables in the single-thread world, `ProcessPrng`, `GetUserProfileDirectoryW`, `RtlNtStatusToDosError`, and `NtReadFile`/`NtWriteFile` over the virtual drive. Ripgrep reached `entry` on that surface (the later CMPXCHG8B work moved its stop); SuperTux was then at 190/722 on bundled DLLs, before sidecar mapping.
- A host-entry callee RET that pops a bound caller resume point (the instruction after `call`) stays in the WASM tier. The return map now publishes each `call_cross` return site to the caller's table slot, and `startAtRip` (RET-map continuation only — not jmp/jcc) starts that module at the block `ripBase` names.
- `fistp` now rounds the way `x87RoundToInt` does under the default control word (nearest-even), not WASM `trunc_s` toward zero: 1.5 stores 2, not 1. Reverse arith `fsubr`/`fdivr` (including dest-top `D8 E9` and dest-`st(i)` `DC E1`/`DC F1`) emit `y-x` / `y/x` instead of throwing from a compile that `nodeSupport` had already accepted.
- Compiled the interpreter-served x87 subset on the WASM tier at the oracle's IEEE-double precision: `fld1`/`fldz`/`fldpi`, `faddp`/`fmulp` (and the rest of the pop-arith family), `fild`/`fistp`, `fld`/`fstp` m32/m64. A CRT-shaped `fld1; fld1; faddp; fstp qword` and `fild; fmulp; fistp` of 6×7 now compile whole instead of handing back as `unsupported_op`.
- Compiled `cpuid` (`0F A2`) on the WASM tier, bit-exact with `executeCpuid`: leaf 0 is the "GenuineIntel" vendor string, leaf 1 is the family/model plus the edx feature bits this pipeline actually implements, every other leaf zeros. The CRT's feature probe no longer hands back as `unsupported_op`.
- Compiled `leave` (`C9`) on the WASM tier as frame teardown, not a transfer: `mov rsp, rbp; pop rbp`, then the following `ret` runs in the same block. Decode no longer splits a basic block on it (`control: "none"`), so a function epilogue compiles whole instead of handing back as `control_leave`.
- Compiled `cmpxchg` (`0F B0`/`0F B1`) on the WASM tier, bit-exact against the interpreter oracle. The CRT's interlocked compare-exchange was a named `unsupported_op` hand-back even though the decoder and `executeCmpxchg` already served it; the emitter now writes dest from src when the accumulator matches, otherwise writes the accumulator from dest, with SUB flags on `(acc, dest)`.
- Landed two grind commands that turn "is the execution surface full?" into an exit code instead of a claim. `node tool/fullness.mjs` recomputes three legs (corpus images at `entry`, WASM-tier residency under 1% interpreter, live GPU adapter) against a denominator this run measures; `node tool/residency.mjs` asks only the WASM leg so a loop can grind on it. `--help` prints the question each file answers; `--json` emits the report; a missing stage or a usage error is a named exit (1 red, 2 usage) rather than a throw. Neither command writes `passing`.
- Carried the direction flag through the tier and compiled the whole string family, REP counted loop included: the module seeds and commits a df byte beside the six flag bytes, `cld`/`std` write it, and `movs`/`stos`/`lods` without a repeat prefix compile as a single element stepping rsi/rdi by the df-chosen delta. The REP counted loop compiles too — and the honest hand-back an earlier revision of this entry recorded was TWO real defects, both now fixed rather than worked around. First, its step charge was inverted: the guard gated the whole `rcx - 1` add on `rcx == 0`, so an empty rep was charged one short and a full one skipped the charge entirely, walking the tier's budget past where the interpreter's stops — the charge is now the block's one plus `(rcx - 1) + (rcx == 0)`, which the oracle's own element count confirms case by case. Second and unrelated to the rep, the decode mapped `0xF8`/`0xF9` to `cld`/`std`: those opcodes are `clc`/`stc` (the CARRY flag), the direction flag is `0xFC`/`0xFD`, so a guest `clc` was silently clearing DF and leaving CF untouched, and the size-less `cld`/`std` node made `executeExtra`'s `sizeMask(undefined)` throw — the interpreter could not execute its own `cld`/`std` at all, which is why the backward-`std` test had been dropped instead of fixed. Both opcodes now lift to their own node with a `clc`/`stc` executor and a tier emitter that writes CF alone. With the charge and the flags correct, the WASM tier is bit-exact against pure interpretation on the real Chocolate Doom run to 4,000,396 instruction across 41,394 invocations (registers, flags, rip, stop reason, and every region byte), and the dropped backward case is restored. `cmps`/`scas` still refuse as `string_compare` (their per-element ZF exit is the loop shape's gap). Also fixed a latent collision this lane's audit surfaced: the double-shift and multiply emitters' scratch local overlapped the multi-region address-translation local, and the i32 local map is re-based to carry df cleanly. The optional `wasmAudit` per-invocation check is now bounded (a 24-invocation sample) and reports `audit_replay_unreachable` rather than throwing: the module serves HLE imports in-module, so a bare re-interpretation from a resident entry treats an import thunk's `ret` as unwinding the outer frame and cannot reproduce the module's resume rip — a limit of that check, not a divergence, and the anchored whole-run comparison covers it in 1.5 s where the audit cost minutes. Eligibility: PuTTY 96.8% to 96.9%; Chocolate Doom holds at 96.2%.
- Served the breakpoint trap (`int3`, opcode 0xCC) end to end. The assemblers pad function gaps with it, and refusing it made every int3-padded candidate function ineligible: the decode now lifts it, the oracle and every interpreter-tier executor fault ON it (`breakpoint`, rip at the instruction, nothing charged), and the tier compiles it to the structured pre-instruction stop — the block's body runs and is charged, the module reports the int3's own address as the resume rip, and the interpreter continues there and faults. An executed int3 is bit-exact between the tiered run and pure interpretation, and a padded function compiles whole with the padding never executing. Eligibility on the real corpus binaries: PuTTY 91.0% to 96.8%; Chocolate Doom holds at 96.2% (its padding was already covered by the segment-prefixed NOP fix).
- Compiled the remaining video-codec SSE shapes and the double-precision shift: `pmulhuw` (two unsigned half-multiplies feeding the never-saturating unsigned narrow back into lane order), `pmaddwd` (exactly WASM's i32x4 dot product), the float unpacks `unpcklps`/`unpckhps`/`unpcklpd`/`unpckhpd` (the integer punpck's interleave over 32/64-bit lanes — decode-only, the emitter already carried it), and `shld`/`shrd` at 32/64 bits (the 16-bit width stays an honest named hand-back: its masked count can exceed the width and reverse the fill direction). Every new WASM SIMD opcode byte was verified against Node's own validator with known-answer lane values before use. Eligibility on the real corpus binaries: PuTTY 90.2% to 91.0%, Chocolate Doom 95.8% to 96.2%.
- Lifted the WASM tier over the shapes the compilers emit most and the tier refused: the one-operand `mul`/`imul` accumulator pair (the full high half is a result now, built from four 32x32 partial products at 64 bits), the bit-test family (`bt`/`bts`/`btr`/`btc` — register offsets wrap within the operand width, memory bases treat the register offset as signed and address whole bytes away), the bit scans (`bsf`/`bsr` through WASM's own ctz/clz, with the zero source keeping the destination), `xadd`, and `xchg` — every case proved bit-exact against the interpreter oracle by the microprogram suite. And the tier no longer refuses the SEGMENT-PREFIXED multi-byte alignment NOP (2E/3E `0F 1F` /0) the assemblers pad every function with: a segment prefix on a pure hint changes nothing architecturally, so it lifts as the nop it is. Measured with the eligibility instrument on the real corpus binaries, candidate function entry that compiles whole went from 79.5% to 90.2% on PuTTY and from 75.5% to 95.8% on Chocolate Doom. The coverage items now name the raw `opcode`/`opcodeText` behind every `not_served` hand-back, so the next blocker is measurable without a debugger.
- Served the kernel32 synchronization breadth the single-thread containment world can carry faithfully (extends BPTK-010/101): the multi-object `WaitForMultipleObjects`/`WaitForMultipleObjectsEx` over the real object table with wait-any index and wait-all consume semantics (the old already-signaled stub is gone), the waitable-timer trio (`CreateWaitableTimerA|W`, `SetWaitableTimer`, `CancelWaitableTimer`, `OpenWaitableTimerA|W`) whose due time is the one wake the virtual clock can deliver deterministically, the named-object open family (`OpenEventA|W`, `OpenMutexA|W`, `OpenSemaphoreA|W` — `CreateSemaphoreA|W` now accepts a name, and `CreateEventA` reads its ANSI name as ANSI), the interlocked bitwise family (`InterlockedAnd/Or/Xor`, the pointer-family CAS and exchange, and a named refusal for `InterlockedCompareExchange64` whose 64-bit prior value cannot cross the EAX dispatch boundary), `SignalObjectAndWait`, the futex-style `WaitOnAddress`/`WakeByAddressAll|Single`, `SleepEx` and `SwitchToThread`, and the thread-exit rows the one live thread can carry (`ExitThread`, `GetExitCodeThread`, `FreeLibraryAndExitThread`). An infinite wait still names the world (`hle_wait_deadlock`) instead of spinning. The conformance case table carries one case per new export and the behavior tests pin the prior-value, consume, and timeout semantics the oracle does not inspect.
- Made the WASM tier share ONE guest memory with the interpreter, and measured what that is worth. `seedStatePlan` used to copy every region's byte into the WASM instance on every invocation and splice them back out, so one tier call cost O(total guest memory map) rather than O(instruction executed). `buildGuestContext64` now allocates every region as a view into one `WebAssembly.Memory` that the compiled module IMPORTS, so a tier transition is a register and rip handoff. On PuTTY startup to its frontier the tiered path went from 5921 ms to 646 ms against 256-285 ms interpreted — 23x slower becomes 2.3x slower — and the handoff itself measures 1.01x. The memory cannot grow (`maximum === initial`), because a grow would detach every region view. Sharing also removed the "discard a faulted run" safety net, so a fault now stops AT the offending access having performed none of its effects and is committable. Records in [the throughput log](doc/tier-throughput-log.md).
- Compiled an indirect `jmp`/`call` as a return-to-dispatch terminator instead of rejecting the whole function, taking Chocolate Doom's WASM-tier eligibility from 285/1155 (24.7%) to 667/1155 (57.7%) of candidate function entry. An indirect transfer is a PRE-TRANSFER stop: the block body runs at WASM speed, then the module stops before the transfer having performed none of its effects and reports that instruction's VA, because only the interpreter can tell an import slot from an ordinary pointer — dispatching an import as a plain call had produced an 8-byte `rsp` divergence against real PuTTY. Every tier exit now reports a resume rip.
- Drove the CRT-initializer, dialog-creation and message-pump specializations in the tiered runner instead of refusing them by symbol name (`lib/exec64.mjs` additively exports `serveImportStop64`, `resumeSpecialization64` and `specializationSentinel64`). Chocolate Doom went from stopping after **164 instruction** with `tier_unsupported_specialization` to running freely on the tier (measured on an idle machine it runs at 0.97x of pure interpretation — close, but the tier does not beat the interpreter on Doom), and PuTTY's frontier moved from a tier refusal to the real `ole32!CoInitialize` import — the same stop `runImage64` reaches, now pinned in the 1:1 test against the reference interpreter so a tier limitation cannot quietly become the frontier again.
- Committed the WASM-tier eligibility instrument (`tool/eligibility.mjs`) and the throughput log, so two number this project had been quoting from session memory are reproducible. Eligibility is a codegen measurement, not correctness and not playability. The log also derives what real-time playability actually costs — ~400,000 instruction per presented frame steady state, so 35 fps needs ~14 M ips against ~0.22 M ips today, a ~64x gap — and records at a 2,000,000-instruction Doom budget that residency, not eligibility or compile cost, was the binding constraint: 98.3% of the executed instruction ran in the interpreter. The log's later 120,000,000-instruction section retires that blocker (residency falls to 0.27%) and moves the binding constraint to the host round trip per invocation.

### Fixed

- Counted a corpus record that never left `staged` as below-entry in `tool/fullness.mjs` `corpusLeg`. The CPU leg used to drop those rows before partitioning, so one image at `entry` plus a sibling stuck on `integrity_mismatch` scanned green.
- Retired three decode defects a systematic sweep found, each a silent wrong answer rather than a crash. `rcl`/`rcr` (shift group 2 /2 and /3) decoded `served: true` with an op no executor handles, so the interpreter — the correctness oracle — faulted on its own decode while the tier had ALREADY refused the same gap as `rotate_rcl`; they now refuse, and the refusal returns null rather than an unsupported node because `decodeStructured` wraps any non-null body with `served: true` and would have promoted it back. `0F 1E` (NOP Ev) consumed only its ModRM byte, reporting length 3 for a form whose hardware length is 8, so the five left-over bytes decoded as fresh instructions (`00 10` is `add [rax],dl`) — a wrong answer AND a resume desync; it now consumes the full ModRM+SIB+disp operand like its `0F 1F` sibling, in both `lib/lift64.mjs` and `lib/x64decode.mjs`, and the refusal path's length is corrected to the cursor's own consumed length. And `A9 id` at 64 bits did not sign-extend its imm32, so `test rax, 0x80000000` ANDed against `0x0000000080000000` instead of `0xffffffff80000000`: ZF came out true where hardware gives false, inverting the branch after it, while every neighbouring accumulator form already wrapped with `toSigned(imm(4), 32)`. Found by sweeping every one-byte and 0F opcode across prefix and ModRM combinations through the real oracle; four defects surfaced, three fixed here. The fourth — the `67` address-size override is accepted and ignored — is NOT fixed by refusal, which was tried and reverted: the Windows x64 CRT reaches `65 67 48 8b 00` (`mov rax, gs:[eax]`) at instruction 14 of Chocolate Doom, so refusing `67` stopped every real binary at its first TEB read. It needs a real 32-bit addressing path, documented in place in `lib/lift64.mjs`. Gate: 768 tests pass, Doom 1:1 bit-exact.
- Made the direction flag and the SSE register file visible to the state comparisons, which is how a decode bug that silently cleared DF survived a suite that otherwise pinned every register and flag. `df` is a machine field rather than a `flagSource` derivation, so `flags()` never returned it and no comparison surface — the tier audit, the whole-run bit-exact tests, the microprogram suite — could see it; all three machines' `flags()` now include it, and a test asserting `df === false` after a `clc` was vacuous until it set DF first. The run surface likewise exposed registers, flags, rip, stop reason and region bytes but not `xmm`, on a comparison whose subject is an SSE-heavy game, so `runImage64` and `runTieredImage` now both return the SSE file and the tiered 1:1 test compares all sixteen. Doom stays bit-exact at 4,000,396 instruction across 41,394 WASM-tier invocations, now with the SSE file under comparison rather than assumed.
- Stopped the path validator from failing `npm run gate` on any machine that had actually run the browser test. `web/payload/` holds the browser-test payload — real game executables the display layer runs — and `.gitignore` declares it out of git for exactly that reason (R-003), but the validator walked the whole tree and rejected every binary it found there, so the gate passed on a clean checkout and failed on a working one. It is now skipped the same way the other out-of-git trees (`.opencode`, `.claude`, `node_modules`) already are; nothing tracked is exempted, since the exemption is a directory name under `web/`, not a pattern. Verified with the payload present: exit 0, 768 tests pass.

- Stopped a corpus-gated test from passing vacuously, which had let a real regression reach review marked green. Seven test file pinned the corpus to one machine's absolute home path, so a staged corpus was invisible anywhere else; a linked git worktree resolved `../bptk-corpus` inside `.claude/worktrees` and found nothing, so every delegated agent's gate ran blind to the real-binary oracle; and `test/lift64.test.mjs` and `test/x64decode.test.mjs` guarded their corpus case with a bare `return`, which node:test counts as a PASS. All now resolve through `lib/corpus.mjs` `resolveStageDir()` — which recovers the main checkout from a worktree's `.git` file — and report a named skip when a payload is absent. Verified both directions: corpus staged, all run with zero skip; corpus hidden, named skip and the run still exits 0.

- Executed real modern x86-64 freeware end to end through the Win64 HLE (BPTK-010, BPTK-031): the bounded interpreter runs a PE32+ image from its CRT entry through WinMain and the game loop. Plink runs entry to `process_exit` bit-identical on macOS and Windows (1,053,075 instruction), PuTTY reaches `user32!CreateDialogParamA` and paints its own "About PuTTY" dialog, and Chocolate Doom loads the 28 MB Freedoom WAD and renders the title screen, the menu under a live keypress, and the in-level 3D view with the full HUD. A played frame is not proven real-time playability, and `passing` is unchanged by this work — it had already moved 0 to 1 on the BPTK-001 approval decision record (commit `211ad1c`); the record lives in [the execution log](doc/execution-log.md) and the per-lane graphics/CPU logs.
- Proved the WASM recompilation fast path bit-exact against the interpreter (BPTK-136): `lib/wasm64.mjs` compiles lifted x86-64 to real WebAssembly over a compact multi-region 64-bit guest space, and `lib/tier.mjs` / `lib/tierrun.mjs` mix the WASM tier with the interpreter over one shared guest state, syncing at function boundaries. A real PuTTY x64 run through ~26,400 instruction of CRT startup to `CreateDialogParamA` is bit-exact against pure interpretation — registers, flags, rip, stop reason, and all region memory — at roughly 80x the interpreter throughput ON COMPILED CODE IN ISOLATION, with a structured refusal for the specialization resume paths the tier does not yet cover. That ratio is not an end-to-end one and was later measured not to hold through `runTieredImage`: see [the throughput log](doc/tier-throughput-log.md). Records in [the runtime-v2 scope](doc/runtime-v2-scope.md) and the lane BA/BB/BD logs.
- Ran a Windows PE live in the browser through a resumable, input-driven session (BPTK-059 surface): `lib/live.mjs` `createLiveSession` steps, presents a frame, and injects input over one persistent guest, bit-exact against a monolithic run; `web/host.mjs` drives it on a page under a native ESM import map (no bundler) with a time-budgeted step loop that blits the runtime's own SDL/GDI surface to a 2D canvas and forwards real keyboard and mouse into the guest's SDL queue. Chocolate Doom plays live on the canvas and takes input; PuTTY paints its dialog. See [the browser display scope](doc/browser-display-scope.md) and [the live-session lane log](doc/lane-ca-log.md).
- Widened the bounded i386 probe with the byte-granular integer space and the microprogram conformance suite (BPTK-009): the full two-operand ALU space in byte and dword column with the exact direction semantics, the al,imm8 column, the 0x80/0x82 byte immediate group, byte mov/moffs/test forms, the 0xfe group, cwde/cbw/cdq/cwd with the high-word rule, xchg, pop r/m, pusha/popa, the carry operation, sahf/lahf, wait, xlat, the loop family, cmovcc, the bt/bts/btr/btc family, bsf/bsr, shld/shrd over exact wide intermediates, cmpxchg, xadd, the hint NOP family, and the flat-model segment and lock prefix, while fs/gs, the privileged instruction, and the port family stop as named structured refusal; rep on cmps/scas is served (with the cmps [esi]-versus-[edi] operand bug fixed) under a declared 2^20 iteration bound. The new microprogram suite freezes hand-derived reference machine state for 38 test compared bit-exact through the real run surface, and the declared opcode inventory is proven consistent with the probe over ~560 executed microprogram. A committed decode sweep measures the real corpus .text: Plink i386 87.5% to 94.0% and OpenTTD i386 86.2% to 95.1% decode coverage. The run record lives in [the CPU subsystem log](doc/cpu-log.md); the isolated-commit gate exits 0.
- Added CPUID to the bounded i386 probe over a declared processor identity leaf table (BPTK-009): the vendor string and feature leaf answer deterministically and an undeclared leaf is a structured unsupported stop, so real feature-detection code reads declared values instead of host identity.
- Widened the bounded i386 probe with the operand-size override (BPTK-009): the 16-bit forms of the register push and pop adjust the stack by two byte, 16-bit arithmetic and immediate forms set the 16-bit carry, zero, sign, and overflow flags exactly while the high word of the 32-bit register is preserved, and the rare 16-bit control forms (ret16, call16, jmp16, pushf16, popf16, r/m16 call and jump) stay structured unsupported. Four runtime check cover the semantics through the real run surface.
- Extended the Win32 core HLE with synchronization objects, process open, file enumeration, and the multimedia timer (BPTK-010, BPTK-015, BPTK-039, BPTK-103 surfaces): CreateEventW, SetEvent, ResetEvent, WaitForSingleObject, and WaitForSingleObjectEx implement a real event state machine where a finite wait advances the one guest clock and an infinite wait on a silent event is a structured deadlock stop, OpenProcess opens only the declared single process, FindFirstFileExA and FindNextFileA enumerate the virtual drive with the real PATH_NOT_FOUND and no-more-files contract, IsValidLocale validates the declared locale, and winmm timeGetTime, timeBeginPeriod, timeEndPeriod, and timeKillEvent serve the timer period over the one monotonic time source. Twenty-two case bring the conformance suite to 169 over all 118 served export, and the real corpus coverage ledger rose from 100 to 113 covered symbol (OpenTTD i386 serves 109 of 302 import, Plink i386 86 of 142).
- Extended the Win32 core HLE (BPTK-010) with module loading and the memory/time probe: LoadLibraryA/W/ExW serve the declared known-DLL set with real refcount while a no-file-system refusal names any other DLL, FreeLibrary decrements honestly, IsBadReadPtr probes the guest range the way the real API does, Sleep advances the one guest clock only, GetThreadTimes reports deterministic thread time, and system DLL file names resolve to the declared system directory. Fourteen case bring the conformance suite to 147 over all 105 served export, and the real corpus coverage ledger rose from 93 to 100 covered symbol.
- Implemented the locale, codepage, and Unicode conversion slice of the HLE core (BPTK-103, now active red): one declared locale (LCID 0x0409) with ANSI/OEM code page 1252 carrying the real 0x80-0x9F best-fit table serves byte-exact MultiByteToWideChar and WideCharToMultiByte for 1252 and real UTF-8 (65001) both directions with the required-size and insufficient-buffer contracts, plus GetACP, GetOEMCP, GetCPInfo, IsValidCodePage, GetUserDefaultLCID, GetLocaleInfoW, CompareStringW with the equal/less/greater contract, LCMapStringW with real case mapping and sortkey honestly refused, GetStringTypeW with the real CTYPE1 classification, GetTimeZoneInformation at the declared UTC bias, and deterministic GetDateFormatW and GetTimeFormatW over a real SYSTEMTIME token formatter fed by the one guest clock. The double-byte fixture and locale enumeration stay red. 23 locale case bring the conformance suite to 133 over all 96 served export, and the real corpus coverage ledger rose from 79 to 93 covered symbol.
- Implemented the Win32 storage slice of the HLE core (BPTK-015, now active red): one bounded virtual drive in guest memory with real CreateFileW, ReadFile, WriteFile, SetFilePointerEx, SetEndOfFile, FlushFileBuffers, CloseHandle, GetFileType, SetStdHandle semantics and a declared output-only console (GetConsoleMode, GetConsoleCP, ReadConsoleW refused), plus an in-memory advapi32 registry hive under the real predefined roots with RegCreateKeyA, RegOpenKeyA, RegOpenKeyExW, RegQueryValueExA with the MORE_DATA contract, RegSetValueExA, and RegCloseKey; traversal and UNC fail closed, 26 storage conformance case cover the surface, the FIX-008 storage exerciser reaches a normal exit through its golden trace with hash-stable repeats, and the real corpus coverage ledger rose from 62 to 79 covered symbol. The copy-on-write overlay, quota transaction, reload persistence, and OPFS bridge stay red because the browser runtime host does not exist.
- Implemented the Win32 core HLE (BPTK-010, now active red): 67 kernel32 and ole32 export are real emulators over the guest memory model (process and module lookup, heap and virtual memory, time from the one monotonic clock, critical sections, TLS, lock-free list, pointer encoding, standard handle with bounded output capture, environment block, and the COM apartment trio), the probe dispatches guest import call through a bounded thunk page with stdcall frame and fault rollback, TLS callback fire before the entry as their own bounded phase, an image whose every import is served now executes end to end, a partially served image is refused with the exact unserved surface named, and the coverage ledger marks 62 of the real 356 corpus import covered (was zero). All 67 export carry conformance case and the FIX-003 exerciser reaches a normal process exit through its golden call trace.

- Implemented the empirical foundation (GS-104 through GS-107 as BPTK-083 through BPTK-086, now active red): `bptk corpus acquire` verifies the nine-entry lawful freeware manifest byte-stably against its pinned hash with payload staged outside the repository, `bptk corpus run` drives every entry through the real ingest and run path and records the reached stage with a named generic gap mapped to a roadmap item (the committed record and [the empirical report](doc/empirical-corpus-report.md) rank BPTK-031 six times, BPTK-010 twice, BPTK-007 once), `bptk corpus generalize` is the merge gate that refuses a single-beneficiary fix, and `bptk ingest` now stages plain zip payload under the declared bound with path-escape and encrypted-entry refusal.
- Recorded the prototype-or-defer research verdict for every promoted P4 item (BPTK-049, BPTK-057, BPTK-058, BPTK-060, BPTK-072, BPTK-073, BPTK-092, BPTK-145) in [the P4 deferral report](doc/research-p4-deferral.md): every verdict is defer with the evidence already in the repository and the named first prototype step; no verdict expands a support surface and every owning item stays planned and red.

- Implemented the authoring SDK semver contract and the hosting-constraint emission (GS-072, GS-065 as BPTK-116 and BPTK-109, now active red): the committed API manifest is compared against the live package entry exports in the planning gate, so removing or renaming an export fails the merge, and every host emission carries the declared cross-origin header set with the single-thread fallback.

- Implemented the graphics unification core (GS-018, GS-020, GS-022, GS-024, GS-025 as BPTK-059, BPTK-061, BPTK-063, BPTK-065, BPTK-066, now active red): one unified shader representation with exactly one WGSL emitter proven by a static audit in the planning gate, a Direct3D 9 pixel-shader frontend over the real token stream for the shader-model 2 and 3 arithmetic core that names an unsupported opcode instead of dropping it, one API-blind fixed-function generator where identical state yields identical output, a capability report derived from the compiled support table where an inflated advertisement fails by construction, and a frame-diff engine that computes its reference at test time with no committed baseline.

- Implemented the governance gate (GS-078, GS-081, GS-082 as BPTK-121, BPTK-124, BPTK-125, now active red): the planning validator now fails a default branch older than the declared 45-day window, refuses a merge that lowers the frozen test floor or lands an orphan runtime module, and fails a maintainer record missing the non-self review rule, the two-signer release rule, or the recovery drill; the maintainer record and the code-owner rule are repository surface.

- Implemented the conformance apparatus and the export-coverage ledger (GS-036, GS-041 as BPTK-076 and BPTK-081, now active red): one case-table format and oracle comparison engine where a zero-case export is a coverage hole, and `bptk corpus coverage` joins the real corpus import surface against the emulated symbol set — 356 imported symbol across the nine staged entry, every one honestly absent against the declared gap budget.

- Implemented the diagnose-and-route front door, the lane bundle envelope, the web-native ingest, and the honesty label (GS-043, GS-044, GS-048, GS-101 as BPTK-088, BPTK-089, BPTK-093, BPTK-129, now active red): the inspect surface returns a ranked lane decision with evidence-weighted confidence over the eight-lane taxonomy; the run surface loads a schema-2 lane bundle and refuses a lane that mismatches its payload; a web-native export stages only under a declared least-privilege capability manifest with any grant refused; and the run report carries the lane and accuracy label with an explicit not-a-playable-claim flag and the machine-readable unsupported set.

- Implemented the security layer and license graph (GS-084, GS-086, GS-088 through GS-091 as BPTK-127 and BPTK-130, BPTK-132 through BPTK-135, now active red): the protection census covers SafeDisc, SecuROM, LaserLock, StarForce, and Tages on documented static signature and the import boundary refuses a refused-route input before any artifact is written; a bounded pre-execution threat scan quarantines a known-bad input from every staging path; every packaged bundle carries the default least-privilege capability manifest and a declared grant is refused with the surfaced capability report; a bundle naming an absent lane is refused instead of mis-run; the trust tier grades input from byte and evidence alone with no silent upgrade; and the license graph resolves every declared component through one frozen disposition table, refusing unknown, unlicensed, and expired node.

- Recalibrated the declared extraction bound from the corpus finding: the amplification guard now carries an absolute floor (1 MiB) so a lawful small high-ratio entry is not refused, and `ratio_max` moves 32 → 64 with genuine bomb input near 1000:1 still refused and the total output bound unchanged.

- Ratified the gold-standard program into the roadmap: the execution-core pillar (GS-001–013) is promoted as planned red item BPTK-046 through BPTK-058 with benchmark specification BENCH-046 through BENCH-058 in [the promoted planned scope](ROADMAP.md#gold-standard-program--promoted-planned-scope). Promotion is a work-list contract, not implementation: every promoted row stays red and gate-excluded until its behavior lands.
- Ratified the graphics pillar (GS-018–034) as planned red item BPTK-059 through BPTK-075 with benchmark specification BENCH-059 through BENCH-075: one unified shader representation with a single emitter, the fixed-function generator, honest capability report, test-time frame reference, presentation, multisample, hostile-bytecode containment, pipeline cache, and the compute and console-framebuffer research pair.
- Ratified the ecosystem pillar (GS-078–085) as planned red item BPTK-121 through BPTK-128 with benchmark specification BENCH-121 through BENCH-128: the non-stale repository and reproducible release, the public tracker and database export, the ratcheting coverage bar, the multi-maintainer recovery mechanism, the unified contributor gate, the license and provenance graph, and governance of breaking change and analytics.
- Ratified the correctness and runtime-service pillar (GS-036–042, GS-104–108, GS-052–060) as planned red item BPTK-076 through BPTK-087 and BPTK-096 through BPTK-104: the generic conformance suite, captured trace corpus, rating ladder, deterministic reference, evidence-backed database row, export-coverage ledger, false-green self-audit, the real freeware executable corpus with its run harness and generalization gate, non-game program support, the time-travel debugger, and the positional audio, full input, storage, networking, full-motion video, Win32 breadth, optical drive, locale, and frame-pacing service.
- Ratified the lane pillar (GS-043–050, GS-101) as planned red item BPTK-088 through BPTK-095 and BPTK-129: the diagnose-and-route front door, the universal bundle envelope, source build autodetection, the engine-lane runtime host, the modern-PC research lane, web-native ingest, the pre-Win32 emulator lane, cross-lane fallback with recorded reason, and the lane and accuracy honesty label with its machine-readable unsupported set.
- Ratified the platform pillar (GS-061–067) as planned red item BPTK-105 through BPTK-111 with benchmark specification BENCH-105 through BENCH-111: compressed and range-based streaming with prefetch, the four-host embedding SDK, offline install, hosting-constraint automation, cross-title asset deduplication, and overlay-only cloud save synchronization.
- Ratified the library pillar (GS-068–076) as planned red item BPTK-112 through BPTK-120 with benchmark specification BENCH-112 through BENCH-120: the schema-gated catalog, instant-play hosting with grant-gated publication, bring-your-own local import, the first-party title whose run is its evidence, the authoring SDK with its semver contract, the community submission pipeline, the append-only preservation catalog, record-backed rating display, and mod overlay support.
- Ratified the security pillar (GS-086–091) as planned red item BPTK-130 through BPTK-135 with benchmark specification BENCH-130 through BENCH-135: per-lane confinement profile, cross-lane resource bound, named copy-protection refusal with a zero-artifact guarantee, pre-execution threat scan with quarantine, capability-manifest enforcement with consent, and the legal trust-tier boundary.
- Ratified the performance pillar (GS-092–098) as planned red item BPTK-136 through BPTK-142 with benchmark specification BENCH-136 through BENCH-142: the instruction-throughput floor, the recompile-over-interpret and native-fraction speedup, per-lane frame-time budget, cold and warm start budget, memory ceiling with boundary cost, audio and latency bound, and sustained-run stability.
- Ratified the accessibility and reach item (GS-099, GS-100, GS-102) as planned red item BPTK-143 through BPTK-145 with benchmark specification BENCH-143 through BENCH-145: runtime-level accessibility injection, shell conformance at the declared level, and the dead-live-service revival research lane.
- Added `bptk ingest <input> --output <dir>` (and its plan-only form): one command accepts any executable input, classifies and census-routes it, extracts a supported installer payload, packages any plain PE into a synthesized bundle manifest, reports bounded-probe eligibility, and refuses DOS, 16-bit, and archive input with structured reason. Ingestion never executes the input.
- Added the static recompilation fourth lane to `bptk foundation compare` (BPTK-036) with an honest unavailable signal, and a dated no-marketing feasibility note recording the N64Recomp, PSXRecomp, and XenonRecomp prior art, the reusable foundation, and the missing evidence behind the deferral.
- Expanded the bounded i386 probe (BPTK-009): shift and rotate group with exact count and carry behavior, multiply and divide with exact 64-bit intermediate and structured divide_error, two-operand IMUL, setcc, string operation with bounded REP/REPE/REPNE and CLD/STD direction control, and a 64-bit precision x87 subset covering load, store, integer conversion, arithmetic, compare, and control-word service; everything outside the subset stays a structured unsupported stop.
- Added the one monotonic guest clock (BPTK-039): QPC, RDTSC, GetTickCount, timeGetTime, and vertical-blank derivation all read one monotonic base with the one-millisecond tick period, the 32-bit wrap, and a clamped delta so a stalled reader neither jumps ahead nor runs at double speed; the probe serves guest RDTSC deterministically from that base through `bptk run`.
- Added the declared guest containment policy (BPTK-045): the probe confines guest memory to the mapped image and bounded stack, denies host script, file system, network, process, and device capability by absence, reports the policy on every run, and refuses an execution manifest that declares capability outside the policy.
- Added the static middleware, copy-protection, and engine census (BPTK-037 and BPTK-043): `bptk inspect` routes middleware import and SafeDisc loader evidence into handle, warn, extract, or refuse, and fingerprints id Tech 1, id Tech 2, Build, SCUMM, Sierra AGI, and Sierra SCI assets to their named open reimplementation without bundling engine or game code. Proprietary Indeo and WMV codec evidence refuses like the video posture.
- Added `bptk import <input>` installer extraction (BPTK-038): the bounded extractor unpacks the pinned Inno Setup 6 unicode family and an MSCF cabinet with stored or MSZIP folder to a declared output directory, refuses a 16-bit setup stub, an encrypted or BZip2 or LZMA chunk, a checksum mismatch, and a payload path escape, and never executes installer code. Plan-only import reports the payload without writing.
- Added the declared resource-bound layer (BPTK-044): one frozen bound caps decompressed output, amplification ratio, entry count, nesting depth, and compressed chunk read for extraction, and the probe enforces the declared instruction budget; a decompression bomb is refused before its output is allocated.
- Added `legal`, `corpus status`, `security`, and `inspect` as bounded local-analysis commands.
- Added safe path traversal with entry, depth, and file-size limits; symbolic-link roots are rejected and nested symbolic links are never followed.
- Added PE32, source-project, archive, installer-container, engine-asset, folder, and unknown-file classification without execution or upload.
- Added temporary-input Tier 1 checks for PE32 classification, source classification, and symbolic-link refusal without committed fixtures or retained results.
- Added `benchmark` with deterministic in-memory checks and no retained result artifact.
- Added `foundation compare` with measured input and local-toolchain signals.
- Added `port --source` diagnosis that reports missing Emscripten and browser-adapter work without treating a scaffold as a port.
- Added `run` PE32 mapping for bounded header, section, relocation, import-directory, TLS-directory, stack, heap, and entry-point inspection without guest execution.
- Added deterministic PE32 static-image resolution for declared named and ordinal import, bounded TLS callback metadata, executable entry-point validation, and precise malformed-range rejection through `bptk run`.
- Added `doctor --graphics`, which launches the installed Chrome against an ephemeral data document and observes WebGPU, WebGL2, WebGL, AudioContext, isolation, SharedArrayBuffer, and renderer state.
- Added atomic content-addressed asset packaging with one-megabyte chunk deduplication and no bundled game runtime.
- Added HTML and React host source that preserves one package identity without moving a game loop into React.
- Added local report modes where `off` writes nothing and `consent` writes one privacy-minimized package/environment report.
- Added package-integrity performance timing without presenting asset I/O as game-runtime performance.
- Added live isolation-based thread selection, zero-attempt network policy, and local control-profile validation.
- Added safe engine-adapter diagnosis that copies no asset without rights and adapter approval.
- Added browser-profile diagnosis that observes installed Chrome while refusing compatibility claims without a game runtime.
- Added temporary-package Tier 1 checks proving network off and prompt modes make zero attempts.
- Added read-only save-boundary diagnosis with strict profile validation and no fabricated persistence.
- Added safe import-and-run diagnosis that neither stages nor executes an asset package.
- Added bounded x86-64, D3D11, and modern research surfaces that combine input signal with live browser capability and return an explicit defer decision.
- Added temporary-package Tier 1 checks for save path refusal and import non-execution.
- Added an opt-in `i386_probe_v1` package profile that deterministically runs a bounded integer instruction subset at the mapped PE32 entry point with structured stop state.
- Added disposable real-CLI regression checks for arithmetic and flag behavior, branch and call flow, memory access, self-modification, exact instruction budget, deterministic state, unsupported x87, import and TLS refusal, and memory fault.
- Added instruction-atomic fault rollback, faulting-address diagnostics, bounded no-follow executable and package-manifest reads, and one global import-entry allocation budget.

### Changed

- Promoted BPTK-007 to implemented-but-red while its approval, corpus, foundation, and source-pipeline prerequisite remain red.
- Replaced the self-referential git revision in `data/status.json` with a deterministic manifest content revision so a product checkpoint remains valid before and after commit.
- Promoted BPTK-003 to implemented-but-red and expanded the exact dependency-free package inventory from 10 to 21 files.
- Promoted BPTK-017 to implemented-but-red and expanded the exact dependency-free package inventory to 22 files.
- Expanded the exact dependency-free package inventory to 24 files; packaging and report surfaces remain red until their runtime prerequisite exist.
- Expanded the exact dependency-free package inventory to 27 files; platform diagnostics remain planned and red until their runtime prerequisite exist.
- Expanded the exact dependency-free package inventory to 30 files; save, integration, and research behavior remain red until their execution or prototype prerequisite exist.
- Promoted BPTK-008 to implemented-but-red after its disposable A-J product walk passed; foundation, importer, and approved corpus evidence remain red.
- Expanded the exact dependency-free package inventory to 32 files for the i386 probe checkpoint; BPTK-009 remains planned, excluded, and red.

### Fixed

- Encoded `utf16le` / `ucs2` in the browser `node:buffer` shim (`web/shim/buffer.mjs`), which had no wide-string case and fell through to the one-byte-per-char latin1 path, so `Buffer.from(string, "utf16le")` wrote half the bytes. Every guest wide string (module filename, wide argv, config path) then read back garbled every other byte, and a browser-run WAD-backed guest exited having found no IWAD on a mangled path compare. The shim-versus-`node:buffer` codec test now covers the `utf16le` / `ucs2` roundtrip it had silently omitted.
- Accepted any `ArrayBuffer` view as a staged host file, not only a Node `Buffer` (`lib/hle.mjs`): the browser reads files through `fetch().arrayBuffer()`, yielding a plain `Uint8Array` that the `Buffer.isBuffer` guard was silently dropping. A non-Buffer view is now normalized to a zero-copy `Buffer` over the same memory and reads identically.
- Surfaced browser-host module-graph boot errors on the page and shimmed `node:perf_hooks`, so the runtime's import graph loads under a native import map instead of failing opaquely.

### Known gap

- BPTK-002 and BPTK-004 still require genuine named review; command output does not constitute approval. BPTK-001 moved off that list on a maintainer decision record — the outbound licence and upstream reuse posture were approved on 2026-09-07 (commit `211ad1c`, `passing` 0 to 1, `data/status.json` `"passing": 1`) — while its runtime-component and public-product-name clearance stay deferred to a runtime release exactly as `doc/legal-boundary.md` already required.
- A real Windows game now executes and renders and takes live input (Chocolate Doom), but at interpreter speed (~0.2 to 0.4M instruction per second, seconds per frame) — no game-runtime benchmark passes, so real-time playability is not proven and the one passing item remains the BPTK-001 approval, not a title. Closing the speed gap is coverage on the already-1:1 WASM tier — the per-invocation host round trip the throughput log now names as the binding constraint, with `ret_out_of_region` 58.7% of what is left at a 120,000,000-instruction budget — not a rewrite.
- Full SDL/OpenGL source output, complete i386 and FPU behavior, Win32 API behavior behind declared import binding, and the C++ MSVC STL ABI (the wall for the C++ titles, OpenTTD and Dwarf Fortress) remain weeks-scale.
- The current Chrome profile exposes WebGL2 but lacks cross-origin isolation; Firefox and the remaining declared browser profile have not been observed.
- The live matrix walk observed Chrome 150 as WebGL2-only and unisolated, found Safari installed but unautomated, and found Firefox absent; no browser support state is claimed.

## 0.1.0-alpha.0 — 2026-07-19

### Added — 2026-07-18

- Established BPTK as a proposed Maphy Technologies browser-porting workbench.
- Added a fresh prior-art and browser-constraint audit with pinned upstream revision.
- Added an architecture covering binary, source-assisted, and engine-adapter lane.
- Added the legal and distribution boundary that separates technical feasibility from permitted reuse.
- Added a five-tier roadmap with 33 independently testable item.
- Added one quarantined red benchmark specification for every accepted roadmap item.
- Added a standard-library validator for count, dependency, benchmark, link, promotion, naming, and planning-scope integrity.
- Proved the validator red-to-green with a temporary benchmark-owner mismatch and restored the valid state.
- Added the complete 46-candidate origin and deduplication ledger and made the validator reconcile it with every accepted, rejected, and deferred target.
- Added authoritative browser evidence for autoplay, OPFS quota and eviction, file access, worker CSP, fullscreen, pointer lock, gamepad, and WCAG 2.2.
- Added precommitted numeric-threshold ownership to BPTK-002, an integrated P1 first-playable fixture to BPTK-016, and a shared binary/source package gate to BPTK-024.
- Separated compatibility state from evidence provenance and made client-side imported-game execution an explicit product boundary.
- Added validator checks for ROADMAP source scope, candidate deduplication, threshold consumer, and integration-gate ownership.
- Proved the new source-scope and candidate-target checks red-to-green with temporary mutations and restored the valid state.
- Made BPTK-002 threshold ownership explicit on every runtime specification and added a red-to-green validator mutation proving the rule is enforced.
- Clarified that runtime specifications are future acceptance contracts, not executable benchmark evidence until their runner and fixture land.

### Added — 2026-07-19

- Licensed BPTK-authored repository material under Apache-2.0 and added the Maphy Technologies `NOTICE`.
- Added a third-party inventory and incorporation rule that prevents the project license from being misapplied to upstream component or user-supplied game content.
- Updated BPTK-001 truthfully: repository licensing is recorded, while runtime-component and public-name approval remain open.
- Added contribution licensing and provenance rules for public pull requests.
- Added a least-privilege GitHub Actions workflow for the planning validator with immutable CI-tool pins and provenance.
- Added the public, dependency-free `@bygelo/bptk` roadmap-tooling package with `status`, `doctor`, and library export.
- Added five Node.js CLI test covering truthful default help, deterministic status JSON, bounded diagnostics, text disclaimers, and invalid input.
- Added an exact 10-file npm tarball manifest that excludes source script, test, benchmark material, and `package-lock.json` from distribution.
- Extended the repository gate across Node.js 22 and 24 while retaining the Python roadmap validator.
- Recorded the scoped package-name permission separately from the still-open runtime and public-product-name review.
- Published the pre-alpha on `next`; npm also assigned `latest` during the first package publication, and an authenticated removal returned `400 Bad Request`. Both currently resolve to the exact alpha while installation documentation remains version-pinned.

### Clarified

- “Import broadly” means safe classification and a compatibility report; it is not a claim that every Windows binary runs.
- React is an optional host adapter, while WebAssembly, canvas, WebGPU or WebGL, WebAudio, and browser storage form the execution surface.

### Known gap

- No runtime implementation, executable fixture, browser package, or compatibility result exists yet.
- The future runtime component graph, upstream reuse strategy, and public product name remain P0 decisions; the current planning repository is Apache-2.0.
- The first npm release is published through the local npm CLI with account 2FA; registry provenance attestation is deferred until an OIDC trusted-publishing workflow is reviewed.
